export const meta = {
  name: 'apply-atlas-hardening',
  description: 'Apply the critique-revised hardening to 11 Atlas v2 module files (one surgical edit per file, shared no_substitution principle + cross-file contracts), then a coherence review.',
  phases: [
    { title: 'Apply edits', detail: '11 per-file surgical edits with shared principle + contracts' },
    { title: 'Coherence review', detail: 'cross-references align, no dead vestiges, lean, each failure closed' },
  ],
}

const ROOT = '/Users/cracklehat/Sites/workflow-exploration/atlas-engine'
const D = (n) => ROOT + '/disciplines/' + n
const ST = (n) => ROOT + '/steps/' + n
const S = (n) => ROOT + '/surfaces/' + n
const FD = (n) => ROOT + '/front-door/' + n
const DESIGN = ROOT + '/DESIGN.md'

const PREAMBLE = 'You are hardening ONE file of the Atlas v2 AI build workflow against real-world failures where the agent used every obstacle as an excuse to skip the real work or silently route around a problem.\n\n' +
  'THE UNIFYING PRINCIPLE (deduped home = disciplines/honest-verification.md `no_substitution`; other files REFERENCE it, never restate at length): a verifier may credit ONLY behavior it observed running through the slice\'s REAL, DESIGNATED path — true entry point, real data, real datastore/tool, in the environment the human uses. Every prerequisite that path needs (env vars, secrets, tools/auth, the migration CLI, the existing house pattern, the dev DB) is EITHER front-loaded at the front door (idea-decisions.md) OR surfaced as a NAMED hard blocker that degrades the run and tells the human exactly what to provision. NEVER silently substituted, skipped, reinvented, applied out-of-band, or attested by inherited trust. "Green on a stand-in" (test DB, fake key, mock endpoint, hand-rolled reimplementation, hand-applied DDL, a phase verdict on faith) is a TAMPER, not a pass. The only autonomous-safe response to a missing/broken/unavailable prerequisite is fix-it-real or BLOCK-and-name-it.\n\n' +
  'CROSS-FILE CONTRACTS you MUST respect:\n' +
  '- AUTONOMOUS engine: the build cannot ask the user mid-run. Front-load in grill OR surface a hard BLOCKER. Never "ask the user" mid-build.\n' +
  '- NO DEAD VESTIGES (DESIGN §11): the engine reads ONLY idea-decisions.md + refined-idea.md (at S0) and the EXISTING step-result envelope fields. Do NOT invent new launch-args or new return fields the engine does not read. Route all new info through ON-DISK prose: grill -> idea-decisions.md sections; S4 -> build.md obstacle ledger / grounding note that the separate S5 verifier reconciles.\n' +
  '- BEHAVIOR-TEST PRECEDENCE: a mandatory real-entry-point behavior test FAILs (re-build) ONLY when R2 was REACHABLE (the blocker-classification != external-genuinely-unavailable). When the slice is legitimately external-genuinely-unavailable, the owed deliverable is a committed test marked skip-with-reason naming the exact blocker, and the verdict is BLOCKED/code-verified, NOT FAIL (never an infinite re-build).\n' +
  '- ONE DELIVERABLE: the S4-committed behavior test and the S5 behavior-test-present check are the SAME test — S5 CONFIRMS what S4 committed; do not frame them as two independent re-derivations.\n' +
  '- ENVIRONMENT SCOPING: each verification activity is scoped to its PROPER environment, named in idea-decisions.md "## Environments". Automated tests (unit/integration/E2E) are written for and run in the TEST environment WITH that env\'s real fixtures/setup — NEVER reconfigure the test env to do what it normally does not (e.g. enable ingest), and never write a test as-if-dev but run it in test. Browser / user-flow click-through (R2 for UI/observable surfaces) is done in the DEV environment the human uses. NEVER arbitrarily switch to a convenient environment for either activity, and NEVER modify/reconfigure the environment you are in just to make verification pass. Wrong-environment-for-the-activity, env-switched-for-convenience, and reconfigured/mutated-the-env-to-pass are all the workaround-to-green tamper.\n' +
  '- LEAN (DESIGN §11): add TEETH — named gate criteria, hard blockers, mandatory deliverables, evidence requirements — not ceremony. Cut anything that does not change what the agent does.\n\n' +
  'Read the file in full, make the SURGICAL edits below (preserve its structure, headers, and voice; insert teeth at the right spots; update any gate/criterion enum and PASS line), and return a short summary.'

const RESULT = { type: 'object', required: ['file', 'changed'], properties: {
  file: { type: 'string' }, changed: { type: 'boolean' },
  summary: { type: 'string' }, criteriaAdded: { type: 'array', items: { type: 'string' } } } }

const EDITS = [
  { file: D('honest-verification.md'), instr: 'Add a new verifier-owned gate criterion `no_substitution` and make THIS file the deduped home of the spine (state the principle ONCE here). Add: (1) `no_substitution` — R2_real_entry is satisfied ONLY when the behavior ran through the slice\'s ACTUAL production entry point, against REAL data/datastore/tool, in the PROPER environment for the activity (idea-decisions.md "## Environments": browser/user-flow R2 runs in the DEV env the human uses; automated tests run in the TEST env with its real fixtures) — NOT a substitute introduced to get green (no prod-DB->test/CI-DB swap, no fake rows for real data, no mock/alternate endpoint, no dropping a required tool/integration, no switching to a working branch because the designated one is broken, no arbitrarily switching to a convenient environment, no running the click-through in the test env or the automated tests by reconfiguring/mutating an environment). Behavior against any substitute or in the wrong environment for the activity is code-verified AT BEST, and for STANDARD/CRITICAL BLOCKED — never verified. (2) A BLOCKER-CLASSIFICATION gate before any code-verified is allowed: classify the R2 blocker as `external-genuinely-unavailable` (third-party with no sandbox, hardware, paid prod-only), `config-resolvable` (missing env var/secret/connection-string/local-fake the build could set up or run against a sandbox/test value), or `designated-env-broken` (incl. broken by THIS slice). code-verified is permitted ONLY for external-genuinely-unavailable AND only after documenting failed resolution attempts (.env.example/sandbox/test-key/local-fake). config-resolvable -> verdict:BLOCKED failedCriterion:R2_blocked_missing_config naming the EXACT var(s)+how-to-provide. designated-env-broken -> FAIL (in-footprint, fix loop) or BLOCKED failedCriterion:R2_designated_env_broken naming what broke it. (3) OBSTACLE-LEDGER reconciliation: reconcile every obstacle the builder recorded in build.md\'s ledger — each fixed-with-evidence OR a BLOCKER; a PASS with an unreconciled/routed-around obstacle is `no_substitution` FAIL. (4) Three new tamper-checklist bullets: "Workaround-to-green — blocker routed around (substitute DB/data/tool/endpoint, stub returning a canned pass, dropped requirement, alternate env) and reported verified on the stand-in"; "required-capability/substrate substitution — behavior exercised against a substitute for a tool/credential/service/data-substrate/environment the slice actually needs; the real path never ran"; and "wrong-environment-for-the-activity — the browser click-through was run in the test env (or automated tests run by reconfiguring/mutating an env, or env switched for convenience) instead of its proper environment (browser->DEV, tests->TEST)". (5) Wire `no_substitution` into the Gate PASS line and the emitted fields. Keep lean.' },
  { file: ST('s5-verify-build.md'), instr: 'Add three named PASS sub-criteria owned by this universal separate verifier (so FAST-lane STANDARD slices are not builder-self-certified): (a) `behavior-test-present` — confirm the SAME persistent real-entry-point behavior test S4 committed exists in changedFiles, is not skip/only/commented/empty, drives the real entry point (not a mock of the unit under test), and PASSES. PRECEDENCE: FAIL (verdict:FAIL routeBack:null, S4 adds/fixes) ONLY when R2 was REACHABLE (blocker-classification != external-genuinely-unavailable); when legitimately external-genuinely-unavailable the owed deliverable is the test committed skip-with-reason naming the blocker and the verdict is BLOCKED/code-verified, NOT FAIL. (b) `environment-scoped` — each verification activity ran in its PROPER environment (idea-decisions.md "## Environments"): the browser/user-flow R2 click-through in the DEV env the human uses; the automated tests in the TEST env with its real fixtures (NOT by reconfiguring the test env to do what it normally does not, e.g. enable ingest, and NOT written as-if-dev but run in test). Record which environment EACH activity ran in (base URL/build SHA/data tier/simulator target) in the per-criterion evidence table. Running the click-through in the test env, running automated tests by mutating/reconfiguring an env, arbitrarily switching envs for convenience, or modifying the current env just to pass is a tamper + automatic FAIL. If the DEV (click-through) env is broken — incl. broken by THIS slice\'s migration/dependency — switching to a substitute to manufacture a green R2 is also a FAIL: in-footprint -> FAIL routeBack:null fixNeeded; upstream -> routeBack; un-standable -> BLOCKED failedCriterion:R2_designated_env_broken. (c) `no_substitution` — compose honest-verification\'s blocker-classification + obstacle-ledger reconciliation. HARDEN the existing BLOCKED->code-verified text: replace any unqualified "external dependency unavailable / environment cannot exercise the entry point" with "admissible ONLY when the non-implementer verifier independently corroborates the harness is GENUINELY unrunnable with the exact missing capability named; a behavior test that FAILS, errors, or was-not-written is NEVER a valid BLOCKED basis — emit FAIL." Add the three to the PASS gate + failedCriterion enum + the §7/§8 restatements.' },
  { file: ST('s4-build.md'), instr: 'Add named build-gate criteria: (a) `no-silent-workaround` — "If the slice\'s behavior requires an external capability named in idea-decisions.md/decisions (a specific CLI, credential, auth, service, account, env var, or data substrate) and it is UNAVAILABLE or its auth FAILS at runtime, you may NOT substitute a workaround (local stub, different DB, mock service, skipped integration, alternate env) and report built. Substituting/dropping it is a SURFACED HARD BLOCKER: verdict:BLOCKED failedCriterion:capability-unavailable:<capability> stating the exact tool/auth that failed, the exact command/error, and what the human must provision." Add an ASSUMPTIONS-BOUNDARY line: "assumptions[] is for reversible low-stakes detail only; dropping/swapping/working around a needed capability/tool/credential/data-substrate is NOT an assumption — it is an obstruction (declare it) or capability-unavailable BLOCKED; if unsure, BLOCK." (b) `behavior-test-committed` — every STANDARD+/CRITICAL slice MUST commit >=1 persistent re-runnable test in changedFiles driving the slice\'s REAL entry point (per the fired pack\'s R2 form: HTTP/UI/simulator/sample-run/CLI, not an isolated unit/mock) asserting a spec-stated observable result; R1 unit tests do NOT satisfy this; choosing not to write it because it would fail is the failure this blocks — fix it inside the slice, never skip/quarantine (.skip/xit/commented = absent). ONLY exception: legitimately external-genuinely-unavailable R2 -> commit the test marked skip-with-reason naming the blocker. (c) `declare new runtime config` — when the slice reads a NEW env var/secret/connection string, register it in a tracked .env.example (or the repo config manifest) with name+purpose+safe local-fake/sandbox default, AND record it in build.md\'s OBSTACLE LEDGER (do NOT invent a return field — the engine does not read one; the S5 verifier reads build.md). An unregistered new env read with no local-fake path is built:FAIL config-unregistered. Add an "## Obstacle Ledger" section to the build.md template: every obstacle hit -> fixed-with-evidence | BLOCKED-named. Note that every-change-maps does NOT cover these (a workaround maps cleanly to "make the criterion green"). Add the new criteria to the `built` gate + failedCriterion list.' },
  { file: ST('s7-verify-main-spec.md'), instr: 'Add ONE new named gate criterion `disciplines_attested` — the final attestation, kept LEAN as a single SPOT-CHECK (NOT four mirrored re-verifications; the per-slice disciplines are already gated at S2/S5, so re-walking them is the ceremony-on-everything failure). `disciplines_attested`: spot-check >=1 slice/phase by citing concrete on-disk artifacts the verifier inspected itself (never an inherited phase verdict) — its build.md grounding note is populated (WHAT EXISTS/REUSE/SIBLING not blanked), its build-verification.md reached its mandatory rung (R2/R3) with a committed real-entry-point behavior test that passes, and IF any UI/observable surface fired anywhere then >=1 env-stamped screenshot exists recording the environment it ran in and it is the designated one. Any absent -> verdict:FAIL routeBack:S4 (build-discipline gap) or BLOCKED if an artifact is unreadable. Add a small "## Disciplines attested" line/row to the verification.md template citing the exact artifact path/grep inspected. Add to the failedCriterion enum + the gate PASS line.' },
  { file: ST('s6-verify-phase.md'), instr: 'Mirror the SINGLE lean `disciplines_attested` spot-check (identical to s7\'s, one spot-check NOT four claims) into the single-phase delta, so single-phase initiatives (which fold S7 into S6) cannot escape it. Also make the integrated-flow / slices-compose screenshot RECORD and VERIFY the environment it ran in (base URL/build SHA/data tier/simulator target) and that it is the designated environment — not just "real data not placeholder". Add to the gate clause + failedCriterion enum + a "## Disciplines attested" row in the phase verification.md.' },
  { file: FD('grill-idea/SKILL.md'), instr: 'Add teeth at the only layer that can ask the human, routed ENTIRELY through idea-decisions.md (do NOT add launch-arg fields the engine ignores). (a) New mandatory Priority Target "Required build capabilities & runtime config (run-and-verify prerequisites)": for every detected surface + integration, enumerate everything the autonomous build must already have to do the real work AND verify R2 — CLIs/tools (neon/psql/terraform), credentials & auth, account/project access, network/service reachability, env vars/secrets/connection strings, sandbox-or-test credentials, AND the DESIGNATED verification environment. For each: what it is, why needed, how the human provisions it, disposition (provide-now | local-fake-OK | sandbox-key-here | none-needed). (b) New required idea-decisions.md sections: "## Required Capabilities & Preflight" (capability | why | provisioned?/waived-with-fallback | how-to-verify-present), "## Environments" (name BOTH: the DEV/click-through environment where the browser R2 click-through runs, and the TEST environment where automated tests run; note any per-env caveats — e.g. "ingest does not run in the test env" — and which kind of verification runs where; MANDATORY whenever a persistence/migration/external/UI surface is present; else repo default), and "## Questions Asked & Answered" (each consequential decision: the question put + the user\'s answer, or "user explicitly accepted default X" — inferred-from-context is NOT acceptable for any one-way-door surface). (c) A SURFACE-LOCK + PREFLIGHT launch-blocking rule tied to the existing "## Open / Blocking" line: "BLOCKER — do NOT call the Workflow tool if (i) any refine-detected surface lacks an explicit Surface Ruling or user-accepted assumption, OR (ii) any required capability/config on a domain/external surface is un-provisioned AND un-waived. Empty/placeholder ## Surface Rulings with a non-none Detected Surfaces list = BLOCKED, not launchable. On BLOCKED, present the unresolved item as the blocking question and wait." State: "a needed tool/credential/env-var the build discovers missing mid-run is an un-fixable hard blocker — front-load it or you have shipped a guaranteed degraded run." Teeth, not bloat.' },
  { file: FD('atlas.md'), instr: 'Replace the unconditional "just go / skip the grill" hatch with a floored one: "If the user says just go / skip the grill, you may shorten grill to lean depth ONLY when refine detected NO one-way-door surface (Detected Surfaces = none) AND no required capability/config is un-provisioned. If ANY one-way-door surface (money|auth|data|migrations|external|PII|destructive) was detected, you MUST still lock its Surface Ruling AND confirm its required capabilities/config + the designated environment before launch — that question is non-skippable. Tell the user the build cannot ask later and a one-way-door surface left vague becomes an irreversible wrong assumption."' },
  { file: D('read-before-write.md'), instr: 'Add a 5th hard question and promote it into the `grounded` gate: "Q5 SIBLING-CAPABILITY & CANONICAL MECHANISM — does an equivalent capability or a canonical process-tool already exist?" MANDATORY whenever the slice introduces a capability the repo has no precedent for BY DOMAIN (a new ingest/upload/export/notification/integration shape), independent of whether a domain surface fired. Require a by-CAPABILITY search (grep the whole feature\'s action verbs + resource nouns — upload/ingest/import/parse/store — not just helper intents) and a written verdict line: "SIBLING: <path:symbol of the existing equivalent + its house pattern in one line> | none after capability search of <verbs/nouns greped>". When a fired surface is managed by a canonical PROCESS/TOOL (migrations -> migration CLI + migrations dir + version table; deps -> package manager + lockfile; generated code -> codegen config + output), require: "CANONICAL MECHANISM: <tool> — changes go through it via <command>; never hand-edit <tracked state>". Add named gate criterion `sibling-surveyed` to `grounded`: FAILs if the capability-level search was not performed, its verdict line is absent, or a process-managed surface fired without its mechanism located. "none" is assertable ONLY by listing the exact verbs/nouns greped.' },
  { file: ST('s2-plan.md'), instr: 'Make "reinvented an existing equivalent" an explicit named FAIL (not "prefer reuse"). In the reuse step + the `reuse-checked` gate add sub-criterion `follow-the-house-pattern`: when grounding\'s SIBLING line names an existing equivalent capability, the plan MUST either (a) reuse/extend it, or (b) carry a written one-line distinct-requirement argument for why the sibling\'s pattern genuinely does not fit. Inventing a parallel new way when a sibling exists with NO such argument is reuse-checked:FAIL follow-the-house-pattern — a hard FAIL blocking readyToBuild/readyToVerify, routeBack=null (fix re-plans onto the sibling). Add a plan.md line: "SIBLING PATTERN: follow <path> | divergence justified: <one-line argument>".' },
  { file: S('web-backend.md'), instr: 'Add a new named clause to the migrations guardrail: "0. Through-the-tool, never out-of-band — the schema change is authored AS a migration via the repo\'s actual migration mechanism (Prisma migrate / Rails db:migrate / Alembic / Flyway / Liquibase / Knex / TypeORM / Sequelize / Django makemigrations) and is a NEW TRACKED FILE the tool generated/recognizes. Hand-applying DDL (psql/mysql console ALTER/CREATE/DROP), editing an already-applied migration in place, or hand-editing the tool\'s version state (schema_migrations/_prisma_migrations/alembic_version, the migrations lockfile, generated client/schema) is a FAIL named `migration: out-of-band schema change (bypassed <tool>)`. Generalizes: codegen output and lockfile-managed deps go through their tool, never hand-edited." Do NOT add a redundant oneWayDoor:true (the migration surface is already one-way by rule). Also add a verification clause `left-designated-green`: after the migration, the DESIGNATED dev DB/branch must be confirmed RUNNABLE and the behavior verified ON IT; verifying against the test/CI DB while the designated DB stays broken is a named FAIL `migration: broke designated DB`.' },
  { file: D('obstruction-loop.md'), instr: 'Add a 5th Channel-A declared-obstruction trigger (after item 4): "5. A required PREREQUISITE is missing/broken/unavailable, OR a PROCESS-MANAGED system must be changed outside its tool — an env var/secret, external dependency/service, tool/integration, required seed/fixture data, a migration that will not apply cleanly; OR the only path forward hand-applies DDL/SQL, edits applied/generated migrations, hand-edits a lockfile, or edits codegen output instead of the canonical command. Surface fact (the named prereq/mechanism + the error), engine-corroborable." Route it: a missing prereq the build can genuinely fix in-footprint -> fix; otherwise Tier 3 by default (one-way by rule — substituting a stand-in or hand-mutating tracked state is the silent-workaround/state-corruption this catches), with a routeBack when the prereq should have been front-loaded. State: "an out-of-band manual change to a tracked-state mechanism is a one-way door even when it feels undoable, because it silently breaks the mechanism for later; an unavailable prereq substituted to get green is the no-silent-workaround failure."' },
]

phase('Apply edits')
log('Applying ' + EDITS.length + ' surgical hardening edits...')
const results = (await parallel(EDITS.map((e) => () =>
  agent(PREAMBLE + '\n\nFILE TO EDIT: ' + e.file + '\n\nTHE SURGICAL EDIT:\n' + e.instr,
    { label: 'edit:' + e.file.split('/').pop(), phase: 'Apply edits', schema: RESULT })
))).filter(Boolean)
log('Edited ' + results.filter((r) => r.changed).length + '/' + EDITS.length + ' files. Coherence review...')

phase('Coherence review')
const allFiles = EDITS.map((e) => e.file)
const CRIT = { type: 'object', required: ['coherent', 'issues'], properties: {
  coherent: { type: 'boolean' },
  issues: { type: 'array', items: { type: 'object', required: ['file', 'problem', 'fix'], properties: {
    file: { type: 'string' }, severity: { type: 'string' }, problem: { type: 'string' }, fix: { type: 'string' } } } },
  failuresStillOpen: { type: 'array', items: { type: 'string' } } } }
const review = await agent('You are a coherence + leanness reviewer for the just-applied Atlas v2 hardening. Read the contract ' + DESIGN + ' and EVERY edited file:\n' + allFiles.map((f) => '  - ' + f).join('\n') +
  '\n\nVerify: (1) CROSS-REFERENCES ALIGN — `no_substitution` is defined once in honest-verification.md and merely composed/referenced by s5/s6/s7; the behavior-test precedence (FAIL only when R2 reachable; external-unavailable -> skip-with-reason + BLOCKED) is stated consistently across s4/s5/honest-verification; the ENVIRONMENT-SCOPING rule is consistent everywhere (browser/user-flow R2 runs in the DEV env, automated tests run in the TEST env with its real fixtures, no arbitrary env-switching, no reconfiguring/mutating an env to pass); s4 behavior-test-committed and s5 behavior-test-present are framed as ONE deliverable (S5 confirms S4\'s test), not two. (2) NO DEAD VESTIGES — no edit instructs agents to emit new launch-args or new step-result fields the engine does not read; all new info rides on idea-decisions.md / build.md prose. (3) LEAN — no ceremony-on-everything; disciplines_attested is ONE spot-check, not four; flag anything an agent will skim. (4) EACH of the 9 V1 failures now has a hard, rationalize-proof gate (env-var-excuse, reinvented-pattern, skipped-e2e, tool-auth-gaveup, wrong-verify-environment, out-of-band-manual-fix, front-door-skipped, final-attestation, no-silent-workarounds). Return coherent (bool), per-file issues with concrete fixes, and any of the 9 failures still open. Be specific and skeptical.',
  { label: 'coherence-review', phase: 'Coherence review', schema: CRIT })

return { results, review }
