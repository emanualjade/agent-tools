export const meta = {
  name: 'apply-atlas-dogfood-fixes',
  description: 'Apply the prioritized fixes from the dogfood evaluation across 10 modules: front-door id threading (grill + /atlas + s3 demotion), In-scope⊆AC reconciliation gate (s0/s6/s7), local-persistence down-tier (risk-tiering), consolidated live-browser + state-transition screenshot rule (honest-verification/s5/s6/s7), and lean code comments (s4).',
  phases: [
    { title: 'Apply', detail: '10 per-file edits with shared definitions' },
    { title: 'Review', detail: 'coherence: cross-refs align, no dead vestiges, lean, each fix lands' },
  ],
}

const ROOT = '/Users/cracklehat/Sites/workflow-exploration/atlas-engine'
const ST = (n) => ROOT + '/steps/' + n
const D = (n) => ROOT + '/disciplines/' + n
const FD = (n) => ROOT + '/front-door/' + n

const SHARED = 'SHARED DEFINITIONS — use this EXACT wording wherever your file needs it, so the cross-file homes match:\n\n' +
  '• LOCAL-PERSISTENCE DOWN-TIER (defined in disciplines/risk-tiering.md §2; other files just consume the resulting tier): "A persistence/migration surface fires CRITICAL only when it changes a DURABLE SHAPE or carries cross-/multi-party risk — a new/changed schema, a destructive or backfilling migration, a shared/multi-reader or external/managed datastore. A persistence surface that is SINGLE-USER, ON-DEVICE/LOCAL, ADDITIVE-ONLY (new store/field, no backfill of existing rows), with NO shared readers and NO external migration tooling is STANDARD (FAST lane, R2 floor), NOT CRITICAL — plain CRUD reads/writes against an existing local store carry ordinary rigor. Creation of a new persistent store/schema stays CRITICAL (it is a durable-shape change); routine record reads/writes do not."\n\n' +
  '• STATE-TRANSITION SCREENSHOT RULE (defined in disciplines/honest-verification.md UI/browser section; s5/s6/s7 reference it): "A screenshot asserting a STATE TRANSITION across an action (a reload, navigation, or click that changes state) must be a genuinely FRESH frame captured AFTER the action — and it must NOT be byte-identical to the pre-action frame unless pixel-identity is explicitly expected and stated. Distinguish \'I re-captured and it matched\' from \'I reused the prior frame\'; a byte-identical pre/post pair cited as transition evidence is a tamper."\n\n' +
  '• LIVE-BROWSER CONSOLIDATION (defined in honest-verification.md; s5/s6/s7 reference it): "Per-slice (S5), a UI slice\'s R2 is satisfied by exercising the behavior through its real entry point via the slice\'s real-entry-point TEST (jsdom/component/DOM through the same seam). The LIVE-browser visual confirmation (real browser + env-stamped screenshot in the DEV env) is owned at the PHASE gate (S6, for the phase\'s user-flow) and the INITIATIVE gate (S7, one final pass). A slice MAY capture a live screenshot but is NOT BLOCKED for lacking one — the phase/initiative gate owns the live visual. Probe browser reachability ONCE (early); do not re-litigate a browser block per slice and carry it through every phase."\n\n' +
  '• IN-SCOPE ⊆ AC RECONCILIATION (s0 mints it, s6/s7 re-check): "Every main-spec In-scope clause must map to ≥1 binary acceptance criterion, an explicit recorded reuse-acceptance, OR a logged scope-waiver. An In-scope clause with none of those is a coverage FAIL — never a clean PASS. (The dogfood miss: \'Set the hemisphere\' was In-scope with no AC, so the UI was silently dropped yet the initiative PASSED.)"\n\n' +
  'RUNTIME FACTS: the engine reads only verdict/failedCriterion/routeBack (+ per-step structured fields) and routes on verdict + routeBack; no new launch-args or return fields beyond what already exists. New cross-step info rides on the on-disk artifacts. Lean: teeth, not ceremony; preserve each file\'s structure/voice; surgical edits only.'

const RESULT = { type: 'object', required: ['file', 'changed'], properties: { file: { type: 'string' }, changed: { type: 'boolean' }, summary: { type: 'string' } } }

const EDITS = [
  { file: D('risk-tiering.md'), instr: 'LOCAL-PERSISTENCE DOWN-TIER (P2 efficiency — the dogfood ran ALL 6 slices CRITICAL because IndexedDB = persistence surface, so the FAST lane was never used). In §2 (the domain-surface list, the persistence/migration row) AND §4 (the rubric), add the carve-out using the SHARED wording: a persistence surface is CRITICAL only for a durable-shape change / destructive or backfilling migration / shared-multi-reader / external-managed datastore; a single-user, on-device/local, additive-only persistence surface with no shared readers and no external migration tooling is STANDARD (FAST, R2 floor); new-store/schema CREATION stays CRITICAL; routine record reads/writes against an existing local store do not. Make it a clean sub-rule of the persistence row + a note in the §4 rubric (rule 1 "ANY domain surface → CRITICAL" now reads the persistence surface through this carve-out). Keep the protection for REAL migrations intact and explicit.' },
  { file: D('honest-verification.md'), instr: 'TWO additions (this file is the home for both). (1) STATE-TRANSITION SCREENSHOT RULE — in the UI/browser screenshot section, add the SHARED rule verbatim (a transition screenshot must be a fresh post-action frame, NOT byte-identical to the pre-action frame; a byte-identical pair cited as transition evidence is a tamper). Add "byte-identical-frame-as-transition-evidence" as a bullet in the tamper checklist. (2) LIVE-BROWSER CONSOLIDATION — add the SHARED consolidation rule (per-slice S5 UI R2 met by the real-entry-point TEST; the live browser + env-stamped screenshot owned at the PHASE/INITIATIVE gate; probe browser reachability once; do not carry a per-slice browser BLOCK through every phase). Make clear this does NOT weaken honest verification — the live visual still happens, just once per phase/initiative instead of redundantly per slice.' },
  { file: ST('s0-spec-and-phases.md'), instr: 'IN-SCOPE ⊆ AC RECONCILIATION GATE (P1 coverage — the dogfood shipped "Set the hemisphere" half-built because no AC covered it, yet PASSED). In §2 (acceptance criteria) add a reconciliation step + a new named gate criterion `inscope-covered`: every §1.2 In-scope clause must map to ≥1 binary AC, an explicit recorded reuse-acceptance, or a logged scope-waiver (use the SHARED wording). Add a small "In-scope coverage map" to the artifact (In-scope clause → AC id(s) | reuse-acceptance | waiver). An uncovered In-scope clause is `inscope-covered` FAIL (not a clean PASS). Add it to the S0 gate + failedCriterion list.' },
  { file: ST('s7-verify-main-spec.md'), instr: 'TWO additions. (1) IN-SCOPE ⊆ AC RE-CHECK: add re-verification that every main-spec In-scope clause was AC-covered, reuse-accepted, or waived AND actually delivered — fold it into `disciplines_attested` or add a sibling `inscope-delivered` criterion: an In-scope item neither covered nor waived AND not built is a coverage FAIL, not a clean PASS (cite the In-scope coverage map from s0). (2) Reference the LIVE-BROWSER CONSOLIDATION + STATE-TRANSITION SCREENSHOT RULE (honest-verification): S7 owns the FINAL live-browser pass (one env-stamped capture per cross-phase flow), and any transition screenshot must be a fresh frame, not byte-identical to its pre-action frame.' },
  { file: ST('s6-verify-phase.md'), instr: 'TWO additions. (1) PHASE-GATE LIVE-BROWSER OWNERSHIP: per the honest-verification LIVE-BROWSER CONSOLIDATION, S6 owns the live-browser visual confirmation for the phase\'s user-flow (one env-stamped DEV-browser capture exercising the phase behavior), so per-slice S5 does not need to carry a live-browser BLOCK; reference the rule and the STATE-TRANSITION SCREENSHOT RULE. (2) IN-SCOPE coverage mirror for single-phase: when S7 folds into S6, also run the `inscope-delivered` re-check.' },
  { file: ST('s5-verify-build.md'), instr: 'LIVE-BROWSER CONSOLIDATION at the slice level (P2 efficiency + the screenshot honesty). In §5 / the UI browser section: a UI slice\'s R2 is satisfied by exercising the behavior through its real entry point via the slice\'s real-entry-point TEST (per honest-verification LIVE-BROWSER CONSOLIDATION) — the slice is NOT BLOCKED for lacking its own live screenshot; the live browser + env-stamped screenshot is owned at the PHASE gate (S6) and INITIATIVE gate (S7). Reference the consolidation rule + the STATE-TRANSITION SCREENSHOT RULE (a transition screenshot must be a fresh frame, not byte-identical to its pre-action frame). Do NOT weaken honest verification — R2 through the real entry point is still mandatory; only the redundant per-slice LIVE capture is consolidated.' },
  { file: ST('s3-verify-plan.md'), instr: 'PROVENANCE-BREAK = NOT downgradeable (P1 — in the dogfood, S3 SAW the front-door/build id split and downgraded it to "a path-naming nuance ... recorded as an assumption", so the gate that should have caught the bypass talked past it). Add a rule: a missing or cross-directory decision-log / front-door PROVENANCE break on a slice that touches a DOMAIN surface is NOT a downgradeable assumption — it is a BLOCK or a routeBack:S0 (the build must run under the front-door <id> with idea-decisions.md present at its own dir). S3 may not down-rule a provenance/identity break to a non-blocking assumption when a domain surface is in play; name it `provenance-break` and route it.' },
  { file: FD('grill-idea/SKILL.md'), instr: 'STRENGTHEN ID THREADING (P1 — in the dogfood the build ran under the default "initiative" id instead of the front-door "houseplant-tracker", spawning two orphan-able dirs). In the Handoff / Build Launch Args section, make it a HARD, unmissable rule: when you launch the build by calling the Workflow tool, you MUST pass `args.initiativeId` = the SAME <id> you used for refined-idea.md / idea-decisions.md (NOT the default). State the failure plainly: "if initiativeId is omitted the build runs as \'initiative\', writes to a DIFFERENT atlas/initiatives/ dir, and orphans this decision log — the build then cannot find your rulings by id." Make the args block call out initiativeId as the load-bearing field that binds the front door to the build.' },
  { file: FD('atlas.md'), instr: 'In the /atlas command\'s step 3 (Build / launch), make explicit: when you call the Workflow tool, set `args.initiativeId` to the SAME <id> the refine/grill step used for atlas/initiatives/<id>/ (refined-idea.md + idea-decisions.md) — do NOT let it default to "initiative". Both the front door and the build must run under one <id> so the build reads the decision log by id (the dogfood ran them under different ids and orphaned the front door).' },
  { file: ST('s4-build.md'), instr: 'LEAN CODE — NO SPEC-PROSE BLEED (P3 — the dogfood code carried ~3:1 paperwork:code with near-duplicate ~12-line spec-citation comment headers restating tier/AC/migration rationale inside production source, which rot when the spec moves). Add a short build rule: production code comments explain INTENT / non-obvious WHY at the point it is needed — they do NOT restate the spec, AC numbers, tier/lane rationale, or migration §-references; no near-duplicate per-function boilerplate headers; process/spec rationale lives in build.md, never in source. Keep it one tight bullet under the build/implement section; do not add ceremony.' },
]

phase('Apply')
log('Applying ' + EDITS.length + ' dogfood-fix edits across modules...')
const results = (await parallel(EDITS.map((e) => () =>
  agent('Apply this surgical fix to ONE Atlas v2 module, from the first real dogfood evaluation. Read the file in full first, then edit in place (preserve structure/headers/voice).\n\n' + SHARED + '\n\nFILE: ' + e.file + '\n\nFIX:\n' + e.instr + '\n\nReturn a short summary of what changed.',
    { label: 'fix:' + e.file.split('/').pop(), phase: 'Apply', schema: RESULT })
))).filter(Boolean)
log('Edited ' + results.filter((r) => r.changed).length + '/' + EDITS.length + ' files. Coherence review...')

phase('Review')
const allFiles = EDITS.map((e) => e.file)
const CRIT = { type: 'object', required: ['coherent', 'issues'], properties: {
  coherent: { type: 'boolean' },
  issues: { type: 'array', items: { type: 'object', required: ['file', 'problem', 'fix'], properties: { file: { type: 'string' }, severity: { type: 'string' }, problem: { type: 'string' }, fix: { type: 'string' } } } } } }
const review = await agent('Coherence + leanness review of the just-applied dogfood fixes. Read ' + ROOT + '/DESIGN.md and every edited file:\n' + allFiles.map((f) => '  - ' + f).join('\n') +
  '\n\nVerify: (1) the LOCAL-PERSISTENCE DOWN-TIER is defined ONCE in risk-tiering.md, keeps REAL migrations CRITICAL, and is consistent with how s1/s2/s4/s5 consume the tier; (2) the STATE-TRANSITION SCREENSHOT RULE + LIVE-BROWSER CONSOLIDATION are defined ONCE in honest-verification.md and merely referenced by s5/s6/s7 (not redefined), and do NOT weaken the requirement that R2 runs through the real entry point or that a live browser pass still happens at phase/initiative; (3) the IN-SCOPE⊆AC gate is consistent across s0 (mint) and s6/s7 (re-check); (4) the front-door id threading (grill + atlas + s3 provenance-break) is consistent and the engine S0 already resolves/blocks (do not re-add engine-side claims in modules); (5) NO dead vestiges (no new launch-args/return-fields the engine ignores); (6) lean — no ceremony, no skim-inducing bloat. Return coherent + per-file issues with concrete fixes.',
  { label: 'coherence-review', phase: 'Review', schema: CRIT })

return { results, review }
