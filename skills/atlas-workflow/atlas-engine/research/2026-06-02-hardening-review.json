{
  "rawFindings": 20,
  "confirmed": 15,
  "verdicts": [
    {
      "id": "S0FLOOR-1-SCHEMA-PHASES-REQUIRED-ON-BLOCKED",
      "confirmed": true,
      "severity": "P3",
      "file": "/Users/cracklehat/Sites/workflow-exploration/.claude/workflows/atlas.mjs",
      "reasoning": "The core defect is REAL and verified against actual code. SCHEMA_S0 at atlas.mjs:127-128 sets required:['verdict','phases'], so `phases` is a mandatory top-level field. The PREFLIGHT FLOOR text at line 425 instructs a blocked S0 to \"return verdict:BLOCKED with failedCriterion ... and do NOT write the spec\" but never states the agent must still emit `phases:[]`. An agent that reads \"do not write the spec\" as \"omit the phase map\" returns {verdict:'BLOCKED', failedCriterion:...} with no `phases` key, which fails structured-output validation against SCHEMA_S0 and forces a harness re-prompt — precisely on the path meant to stop cleanly. The fix is pure prompt text (sandbox-valid, no API). The finding's own caveat checks out: `phases` has no minItems (line 127-128 is just type:array), so an explicitly-emitted empty array DOES validate.\n\nHowever I am DOWNGRADING P2 -> P3 and CORRECTING the proposed fix, on three verified grounds:\n\n1) MATERIALITY IS CAPPED AT ONE SELF-HEALING RE-PROMPT, not a regression. The finding itself concedes the floor's degrade path is structurally correct: line 429 records the blocker and returns BEFORE the phase loop; report.ready stays false (no false-ready); BLOCKED S0 has obstruction:null so absorb() does not crash. Crucially the engine consumes phases defensively at line 430 (`const phases = s0.phases || []`), so even a missing field never crashes engine logic. The only cost is friction at the harness structured-output boundary, which self-corrects on re-prompt. That is real but low.\n\n2) PROBABILITY IS LOWER THAN THE FINDING IMPLIES. The authoritative step file's §7 result-envelope block (s0-spec-and-phases.md:224-238) lists `phases:` as a standard always-present return field of the S0 contract (\"structured fields are the contract\"), and §6 (lines 211-213) documents the BLOCKED case generically without ever saying \"omit phases.\" A competent agent following the step file already has strong reason to emit `phases` on BLOCKED. So the ambiguity is isolated to the engine task string alone.\n\n3) THE PROPOSED FIX IS HALF-WRONG ON A VERIFIED FACT. The finding says to \"Mirror the same one line in steps/s0-spec-and-phases.md §6/§7 so the authoritative step file agrees.\" But the PREFLIGHT FLOOR does NOT exist in the step file. I grepped both trees: the only occurrence of \"PREFLIGHT FLOOR\" / \"do NOT write the spec\" / \"bypassed the front door\" is atlas.mjs:425 (corroborated by review-hardening.mjs which describes it as \"an engine S0 PREFLIGHT FLOOR\"). s0-spec-and-phases.md contains no preflight-floor sentence to append to, and its §6/§7 already treat `phases` as a standard envelope field — so the step-file half of the fix targets text that does not exist and would be a phantom edit. The fix should be confined to the single authoritative location of the floor: the engine task string.\n\nNet: confirmed real defect, P3, fix scoped to the engine only.",
      "refinedFix": "Single-location, sandbox-valid (pure prompt text, no API) edit in /Users/cracklehat/Sites/workflow-exploration/.claude/workflows/atlas.mjs, line 425. In the PREFLIGHT FLOOR sentence, change the clause \"and do NOT write the spec.\" to \"and do NOT write the spec — but STILL return phases:[] (SCHEMA_S0 requires the field; emit it empty so the BLOCKED stop validates without a re-prompt).\" Do NOT touch steps/s0-spec-and-phases.md: the preflight floor does not exist there (verified by grep across the whole tree — only atlas.mjs:425 has it), and §6/§7 (lines 211-213, 224-238) already document `phases` as a standard always-present return field, so no mirror edit is possible or needed. Optional belt-and-suspenders (not required, since line 430 already does `s0.phases || []`): leave the engine consumption as-is. The one-line task-string clarification is sufficient and adds teeth without ceremony."
    },
    {
      "id": "S0FLOOR-2-STEPFILE-GATE-CONTRADICTS-FLOOR",
      "confirmed": true,
      "severity": "P2",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s0-spec-and-phases.md",
      "reasoning": "CONFIRMED — real, correctly located, material.\n\nVERIFIED FACTS:\n1. The preflight floor exists ONLY in the engine's inline S0 TASK string at atlas.mjs:425 (the \"PREFLIGHT FLOOR (closes a direct launch that bypassed the front door)\" block instructing return verdict:BLOCKED for a non-none Detected Surface with empty/placeholder Surface Rulings, or an un-provisioned/un-waived domain-surface capability).\n2. The floor IS engine-enforced (not a dead vestige): the engine routes on a field it actually reads — atlas.mjs:429 `if (s0.verdict === 'BLOCKED')` records a blocker and returns, halting the initiative. So the gate has teeth IF and ONLY IF the agent emits BLOCKED. Its entire strength is the agent's PASS-vs-BLOCKED choice.\n3. The step file s0-spec-and-phases.md contains NO mention of preflight / Surface Rulings / Required Capabilities anywhere (read in full; §6 gate table lines 198-207 and §7 envelope lines 224-238 have no such criterion). The floor was not propagated to the step file.\n4. The step file is declared authoritative by the engine prompt: atlas.mjs:180-182 (\"YOUR INSTRUCTION SET — read and follow EXACTLY ... Those files are authoritative\"). On an inline-TASK-vs-step-file conflict, the prompt structurally privileges the step file.\n5. The step file actively narrows BLOCKED AGAINST the floor: §6 line 213 (\"a true blocker (a decision genuinely unanswerable, not a deferrable assumption) → BLOCKED\"); §7 line 228 (\"blockers: only genuinely unanswerable decisions\"); §1.2 line 58 (\"Each [Needs-Decision] gets a recorded assumption so the build proceeds ... Never a blocker unless genuinely unanswerable\").\n\nWHY MATERIAL: An unruled surface / un-provisioned capability is NOT an \"unanswerable decision\" — it is a front-door-bypass condition, and §1.2 explicitly tells the agent to convert open questions into recorded assumptions and PASS. A faithful agent resolving the engine-task-vs-authoritative-step-file conflict can therefore down-rule the unruled surface to an assumption and PASS, silently defeating the floor — which is precisely the silent substitution DESIGN non-negotiable #12 (lines 46-54: front-load in grill OR surface as a named hard blocker, never silently assume) forbids, in exactly the direct-launch-bypassed-grill case (DESIGN §12) the floor exists to catch. The floor is only as strong as the weaker of its two instruction sources, and the authoritative one contradicts it. The fresh-context/self-sufficiency bar (DESIGN §11 line 347 dead-vestige rule; line 352 \"fresh-context subagent that has only this file\") confirms the step file is supposed to carry the floor itself — the omission is a gap, not a deliberate engine-only split.\n\nADVERSARIAL CHECKS THAT DID NOT REJECT IT: (a) inline TASK does not reliably override — the prompt names the step file authoritative, so at best floor enforcement is a coin-flip on conflict resolution; (b) the fix is sandbox-valid — pure .md doc edit, no FS/Node/Date/Math API, no new launch-arg and no new return field; it reuses verdict:BLOCKED + phases:[] which the engine already reads (:429-430). failedCriterion is informational only (correct per runtime facts), but the routing key verdict:BLOCKED is honored.",
      "refinedFix": "Add the floor to the authoritative step file so both sources agree; keep it lean (teeth, not ceremony). Sandbox-valid: doc-only edit, reuses the existing verdict:BLOCKED + phases:[] envelope the engine already routes on.\n\nEDIT 1 — insert a short section before §1 (after the §0 modality block):\n## 0.5 Preflight floor (front-door bypass — hard stop, NOT a Needs-Decision)\nBefore writing anything, if `idea-decisions.md` shows `Detected Surfaces` other than `none` with `## Surface Rulings` empty/placeholder/unresolved, OR a domain/external surface whose required capability/config is left un-provisioned AND un-waived under `## Required Capabilities & Preflight`, the launch bypassed the front door. This is a **hard stop**, not a deferrable assumption: `verdict:\"BLOCKED\"`, `failedCriterion` naming the unruled surface / un-provisioned capability, `phases:[]`, write no spec. The human resolves it in grill and re-launches. This is the one carve-out to §1.2 — an unruled one-way-door surface is NOT a Needs-Decision the agent may down-rule to an assumption; assuming it is exactly the silent substitution DESIGN non-negotiable #12 forbids (front-load in grill OR a named hard blocker), and the floor exists to catch a direct launch that skipped grill (DESIGN §12).\n\nEDIT 2 — add a gate row to the §6 table (after `spec-written`):\n| `preflight-floor` | front door not bypassed: no non-`none` Detected Surface with empty/placeholder Surface Rulings, and no domain/external surface capability left un-provisioned+un-waived in Required Capabilities & Preflight. **Failing this is `BLOCKED` (per §0.5), not FAIL/assumption.** |\n\nEDIT 3 — carve the narrowing language so §1.2/§6/§7 can't reabsorb the floor. In §6 line 213 change \"a true blocker (a decision genuinely unanswerable, not a deferrable assumption)\" to \"a true blocker (a decision genuinely unanswerable, not a deferrable assumption) — OR the §0.5 preflight-floor breach (front-door bypass; DESIGN non-negotiable #12 + §12)\"; and in §7 the `blockers` comment \"only genuinely unanswerable decisions\" → \"genuinely unanswerable decisions OR a §0.5 preflight-floor breach\". Optionally restate the §0.5 hard-stop carve-out in the §1.2 Needs-Decision row so a reader of that row alone sees it."
    },
    {
      "id": "DVS-1",
      "confirmed": true,
      "severity": "P2",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/disciplines/honest-verification.md",
      "reasoning": "CONFIRMED. Verified against actual code/text.\n\nENGINE NEVER READS THE THREE FIELDS:\n- ENVELOPE (atlas.mjs:118-124) = verdict, failedCriterion, artifactPath, assumptions, blockers, changedFiles, surfaces, obstruction, routeBack. None of ladderReached/verifiedKind/residualRisk.\n- SCHEMA_VERDICT (atlas.mjs:140-141) = ENVELOPE + fixNeeded + failedCriteria only. This is the exact schema verifyWithFix passes for S3/S5/S6/S7 (line 205). None of the three.\n- `grep ladderReached|verifiedKind|residualRisk atlas.mjs` => exit 1, ZERO matches. The engine code references none of them.\n- Routing (lines 210-231) reads only v.verdict, v.fixNeeded, v.failedCriterion, v.failedCriteria, v.artifactPath, v.routeBack. The fix step is handed only v.artifactPath + v.failedCriterion (line 231-232), not ladderReached — so even fix.md:16's claim that S5 ladderReached is a fix input is loose; the fixer recovers it by re-reading the on-disk artifact.\n\nTHE ASSERTION IS BACKWARDS: honest-verification.md:235-236 says \"These ride on existing envelope fields — no new launch-args or return fields the engine does not read.\" The truth is the opposite: 230-232 declare them as additions to \"the standard step envelope\" (line 227), and they ARE three new return fields the engine does not read = DESIGN §11 dead vestige (DESIGN.md:347 bans dead vestiges; §11 leanness bar). A self-blessing comment asserting the channel is wired, when it is not, is the sharpest dead-vestige-schema violation: it will mislead future authors into thinking the engine consumes them.\n\nROUTING IS NOT BROKEN (finding agrees): the gate at 238-246 collapses to verdict (PASS only at the mandatory rung) + failedCriterion (names the unreached rung on BLOCKED), both of which the engine reads. So this is a vestige/leanness defect, not a correctness break — correct P2.\n\nTHE PROSE CHANNEL IS REAL (finding's claim that the values legitimately live on disk is verified): s5-verify-build.md:277-297 writes ladderReached/verifiedKind/residualRisk[] into the on-disk build-verification.md artifact (lines 292, 296-297), which later steps re-read. honest-verification.md:123 even states \"The engine reads the step verdict. Within a PASS, the prose/structured result MUST distinguish...\" — confirming these distinctions are prose-borne and routing collapses to verdict. Only the RETURN-ENVELOPE copy at 230-236 is dead.\n\nONE REFINEMENT to the proposed fix: the second sentence of 235-236 (the no_substitution / build.md obstacle-ledger reconciliation clause) is ACCURATE and unrelated to the false claim, so it must be preserved, not deleted. My refinedFix removes only the false \"ride on existing envelope fields — no new ... return fields\" assertion and keeps the no_substitution clause. The fix is pure prose — fully sandbox-valid (no fs/Node/Date/Math).\n\nOUT OF SCOPE (noted, not part of DVS-1): the identical dead-envelope declaration recurs in s5-verify-build.md:301-315 and s7-verify-main-spec.md:230-239 (and fix.md:16 treats S5 ladderReached as a passed input). Those are the same vestige in sibling files; fixing only honest-verification.md leaves them, but DVS-1 as scoped is real and material on its own.",
      "refinedFix": "Replace honest-verification.md lines 225-236 with:\n\n## What this step records (on-disk artifact, not the return envelope)\n\nRecord in the on-disk verification artifact (`build-verification.md` for S5, the phase/initiative `verification.md` for S6/S7) — later steps re-read it; the engine does not read these off the return:\n\n```\nladderReached:  \"R0\" | \"R1\" | \"R2\" | \"R3\" | \"R4\"   // highest rung actually executed (verifier steps)\nverifiedKind:   \"verified\" | \"code-verified\"        // code-verified => residual risk listed below\nresidualRisk:   string[]                             // required iff verifiedKind == \"code-verified\"\n```\n\nThese do NOT ride on the return envelope. The engine's verifier schema reads only `verdict` + `failedCriterion` (+ `fixNeeded` / `failedCriteria`); the ladder result reaches the gate by collapsing into `verdict` (PASS only at the mandatory rung) and `failedCriterion` (names the unreached rung on BLOCKED). `no_substitution` reconciliation likewise routes through on-disk prose: the `build.md` obstacle ledger.\n\n(Rationale: deletes the false \"ride on existing envelope fields — no new ... return fields the engine does not read\" assertion — it is exactly backwards; preserves the accurate no_substitution/obstacle-ledger clause. Pure prose, sandbox-safe.)"
    },
    {
      "id": "DVS-2",
      "confirmed": false,
      "severity": "P3",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s7-verify-main-spec.md",
      "reasoning": "REJECTED as framed. The finding's core thesis — that ladderReached/verifiedKind/residualRisk are \"DEAD VESTIGES\" in the DVS-1 class — is contradicted by the actual contract.\n\nFacts verified against code/text:\n1. Engine truly does not read these fields. SCHEMA_VERDICT (atlas.mjs:140-141) carries only fixNeeded + failedCriteria beyond the shared ENVELOPE (lines 118-124). S5/S6/S7 all route through verifyWithFix -> runStep(schema: SCHEMA_VERDICT). So ladderReached/verifiedKind/residualRisk/phaseId are indeed silently dropped. That half of the finding is accurate.\n\n2. BUT they are NOT vestiges. DESIGN §3 line 149 EXPLICITLY names ladderReached as a legitimate \"step-specific field layered on top per the step file,\" and states \"the engine reads only the envelope + declared step-specific fields\" (i.e. it ignores the rest by design). DESIGN §9.2 line 314 EXPLICITLY lists ladderReached among fields agents \"report structurally ... so the orchestrator can act.\" The deduped home, disciplines/honest-verification.md:225-236, legitimizes them verbatim: \"These ride on existing envelope fields — no new launch-args or return fields the engine does not read.\" So these are contract-SANCTIONED honest-reporting riders, not the DVS-1 class (a field the engine once read / an instruction to emit a field for a dead purpose). A dead vestige misrepresents a capability; these correctly represent a no-op-to-engine honesty signal the contract asks for.\n\n3. The proposed fix is actively wrong: moving ladderReached/verifiedKind/residualRisk out of the return block into the artifact section would CONTRADICT DESIGN §3:149 and §9.2:314, which require them as structural step output. It would replace a sanctioned pattern with a new contract violation.\n\n4. The S3 contrast the finding leans on cuts the other way: S3 (s3-verify-plan.md:223-225) omits ladderReached for a substantive reason (\"no running code at plan time\"), not as a generic anti-vestige rule. S5/S6/S7 DO reach rungs, so emitting ladderReached is meaningful, not vestigial.\n\nSALVAGEABLE TRUE SUB-DEFECT (separately worth fixing, but not the DVS class claimed): s7-verify-main-spec.md:228 header literally reads \"## Return envelope (the engine reads this; DESIGN §3)\" sitting directly over fields the engine does NOT read (ladderReached/verifiedKind/residualRisk/shipSafe/receipt). That single header is a genuine accuracy bug — it contradicts §10 and the discipline's own \"no return fields the engine does not read.\" Also, unlike S3 (which carries an honest \"engine reads only this envelope + the named fields\" line at :224), S5/S6/S7 blocks lack that annotation, and S6's phaseId:263 is genuinely redundant (engine already has pid; §3 does not sanction phaseId). These are P4 wording/accuracy nits, not the material dead-vestige defect described. Because the finding's central characterization is incorrect and its fix would break the contract, confirmed=false.",
      "refinedFix": "Do NOT relocate ladderReached/verifiedKind/residualRisk (DESIGN §3:149 and §9.2:314 require them as structural step output; the discipline at honest-verification.md:235 sanctions them as riders). The only real, sandbox-valid fix is a wording correction, no engine/schema change:\n\nIn s7-verify-main-spec.md:228, change the false header\n  \"## Return envelope (the engine reads this; DESIGN §3)\"\nto\n  \"## Return envelope (DESIGN §3 — the engine routes on verdict + routeBack only; the rest is honest structural reporting it does not route on)\".\n\nOptionally, for consistency with S3's existing honest annotation (s3-verify-plan.md:224), append a one-line note under the S5 (after :324), S6 (after :268), and S7 (after :247) code blocks: \"Engine routes on: verdict, routeBack, fixNeeded/failedCriteria (+ S4-only build/riskTier/obstruction). ladderReached/verifiedKind/residualRisk are honest structural reporting the engine ignores (DESIGN §3, §9.2) — not artifact-only; keep them here.\" And drop the redundant phaseId from s6-verify-phase.md:263 (the engine already holds pid and §3 does not list phaseId)."
    },
    {
      "id": "DVS-3",
      "confirmed": true,
      "severity": "P3",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s7-verify-main-spec.md",
      "reasoning": "CONFIRMED, real and material (P3 dead-vestige-schema). Verified against the actual code/text:\n\n1. S7 runs under SCHEMA_VERDICT (atlas.mjs line 205: `runStep({ step, ..., schema: SCHEMA_VERDICT, ... })`). SCHEMA_VERDICT (line 140-141) = `mk({ fixNeeded, failedCriteria })`, and `mk` (line 125) spreads ENVELOPE (lines 118-124) = {verdict, failedCriterion, artifactPath, assumptions, blockers, changedFiles, surfaces, obstruction, routeBack}. There is no `shipSafe` and no `receipt` property anywhere in the S7 schema. A model emitting them produces fields that are never parsed.\n\n2. The engine derives the verifier outcome purely from verdict: verifyWithFix line 208 `if (v.verdict === 'PASS') return { status: 'passed', verdict: v }`; runInitiative line 473 `report.ready = final.status === 'passed'`. Ship-safety reaches the engine only through verdict:PASS (which the file itself already states at line 249), never through a `shipSafe` field. The receipt is already correctly destined for on-disk prose (line 213-214: append to verification.md or write receipt.md). So lines 240-241 are pure duplicate vestiges, exactly the class DESIGN forbids (DESIGN.md line 347: \"No dead vestiges ... no doubled\").\n\n3. The header at line 228 \"(the engine reads this; DESIGN §3)\" is therefore false for `receipt` (engine provably never reads it). The finding correctly scopes to the `shipSafe: boolean` (line 240) and `receipt: {...}` (line 241) return fields and does NOT touch the `failedCriterion` enum value \"shipSafe\" at line 234 — that string is the informational named gate criterion (RUNTIME FACT 2: failedCriterion strings are informational; matches the gate at line 137), so deleting the boolean/object does not break the named gate.\n\nAdversarial caveat (refines, does not reject): the same block also lists `ladderReached` (237), `verifiedKind` (238), and `residualRisk` (239), which are EQUALLY absent from SCHEMA_VERDICT. So the literal proposed-fix instruction \"correct the header to list only the actually-read fields\" is under-scoped — taken literally it would force editing those three too (and several listed names like assumptions/blockers/surfaces/obstruction/routeBack/verdict/failedCriterion/artifactPath ARE read). To stay in-scope and avoid over-editing, the refined fix deletes only the two vestige lines and relabels the header honestly as the verifier's emitted envelope (engine routes on the standard-envelope fields; the evidence mirrors live on disk) rather than claiming the engine reads every line. The defect (a header falsely asserting the engine reads a receipt object it never reads, plus two unparsed fields) is genuine, sandbox-irrelevant (pure prose edit, no FS/Node/Date/Math API touched), and honoring lean (teeth not ceremony) means removing the dead copies.",
      "refinedFix": "In /Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s7-verify-main-spec.md:\n\n(a) Delete the two vestige lines from the return-envelope code block (currently lines 240-241):\n    shipSafe:        boolean\n    receipt:         { shipped: string, runUse: string, next: string[] }\n\n(b) Fix the block header (line 228) so it no longer claims the engine reads every listed field. Replace:\n    ## Return envelope (the engine reads this; DESIGN §3)\nwith:\n    ## Return envelope (the verifier emits this; the engine routes on the standard-envelope fields — verdict/failedCriterion/routeBack/artifactPath/assumptions/blockers/surfaces/obstruction; DESIGN §3)\nThe remaining non-routed lines (ladderReached/verifiedKind/residualRisk) stay as on-disk-evidence mirrors but the header no longer falsely asserts the engine reads them.\n\n(c) Leave line 234's `failedCriterion` enum value \"shipSafe\" untouched — it is the informational named gate criterion, not a return field, and removing it would break the named-gate mapping at line 137.\n\nThis is sandbox-valid: a prose/markdown edit only, no filesystem/Node/Date.now/Math.random or harness-arg dependency."
    },
    {
      "id": "XFC-1",
      "confirmed": true,
      "severity": "P3",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s6-verify-phase.md and /Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s7-verify-main-spec.md",
      "reasoning": "The cross-file contradiction is REAL and verified against the actual text. s6:161-162 and s7:145-146 both require disciplines_attested clause (b) to be \"a committed real-entry-point behavior test THAT PASSES\" with NO exception, and both make \"(a)/(b)/(c) absent -> disciplines_attested FAIL routeBack:S4\" (s6:167 / s7:151). The behavior-test precedence is stated consistently in the opposite direction in three places I confirmed: s4 criterion 7 (s4-build.md:175-178) \"ONLY exception: a slice legitimately external-genuinely-unavailable -> commit the test marked skip-with-reason... verdict then BLOCKED/code-verified, never an infinite re-build\"; s5 (s5-verify-build.md:192-199) \"BLOCKED / code-verified, never FAIL\"; honest-verification.md:83 and :87-91 same. So a legitimately external-genuinely-unavailable slice owes a committed skip-with-reason test (which is, by S5's own definition at :187-190, NOT a passing test) under verdict BLOCKED/code-verified -- it would pass S4/S5 honestly yet trip s6/s7 clause (b) as written. That is a genuine contradiction between the two attestation steps and the three behavior-test sources, and it is material because disciplines_attested is a LIVE gate the engine routes on (routeBack:S4 is a field the engine reads), not a dead vestige -- the fix reads only the on-disk build-verification.md verdict + the committed test, artifacts already exposed. It is teeth, not ceremony.\n\nTWO CORRECTIONS to the finding, which is why I downgrade P2->P3 and rewrite the fix:\n\n(1) The finding's \"infinite-rebuild\" claim is mechanically FALSE. I traced the engine (atlas.mjs). For S7/multi-phase, runInitiative:464-471 only honors a routeBack of targetStep:'S0'; a routeBack:S4 from S7 is NOT re-entered -- it falls through to line 471 and degrades to an \"unhandled final-gate route-back\" blocker. For S6/single-phase fold, the phase loop (441-456) re-enters S1-S4 bounded by maxPhaseReentries then degrades to a blocker (454). Neither path loops infinitely. The true harm is a FALSE-NEGATIVE: an honest external-unavailable slice that correctly PASSED S4/S5 gets spuriously FAILed at attestation and either burns bounded re-entry budget on S4 re-builds that provably cannot yield a passing test (S6) or is immediately converted into a degraded blocker (S7) -- turning a legitimate ship-with-residual-risk outcome into a failure. Still material, but it is a false-fail, not the infinite loop the precedence exists to prevent.\n\n(2) The contradiction is partly self-mitigating: both clause (b)s already append \"(the same one deliverable S4 committed and S5 confirmed)\", and for an external-unavailable slice that \"same one\" IS the skip test. A careful reader resolves clause (b) via that anchor. The defect is that the literal \"that passes\" + \"absent -> FAIL\" still lets a literal-minded verifier wrongly FAIL a compliant artifact. Prose-only fix is warranted.",
      "refinedFix": "Mirror the s4/s5 exception in both attestation clause (b)s. This is prose-only and reads only on-disk artifacts the engine already exposes (build-verification.md verdict + the committed test).\n\nIn /Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s6-verify-phase.md, clause (b) at lines 161-163, replace:\n  \"(b) its `build-verification.md` reached its **mandatory rung (R2/R3)** with a committed **real-entry-point behavior test that passes** (the same one deliverable S4 committed and S5 confirmed — not a re-derivation);\"\nwith:\n  \"(b) its `build-verification.md` reached its **mandatory rung (R2/R3)** with a committed **real-entry-point behavior test that passes** — OR, for a slice S5 recorded `external-genuinely-unavailable`, the **same** test committed `skip`-with-reason naming the exact blocker, with that slice **BLOCKED/`code-verified`** and its residual risk listed (the precedence at s4 criterion 7 / s5 §6.1 / honest-verification.md §Behavior-test precedence — never a re-derivation; a skip-with-reason on any slice NOT so classified is still `disciplines_attested` FAIL);\"\n\nIn /Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s7-verify-main-spec.md, clause (b) at lines 145-147, replace:\n  \"(b) its `build-verification.md` reached its **mandatory rung (R2/R3)** with a committed **real-entry-point behavior test that passes** (same one deliverable S4 committed and S5 confirmed — not a re-derivation);\"\nwith:\n  \"(b) its `build-verification.md` reached its **mandatory rung (R2/R3)** with a committed **real-entry-point behavior test that passes** — OR, for a slice S5 recorded `external-genuinely-unavailable`, the **same** test committed `skip`-with-reason naming the exact blocker, with that slice **BLOCKED/`code-verified`** and its residual risk listed (per the s4 criterion 7 / s5 §6.1 / honest-verification.md precedence — not a re-derivation; a skip-with-reason on any slice NOT so classified is still a FAIL);\"\n\nThe trailing \"Any of (a)/(b)/(c) absent -> FAIL routeBack:S4\" lines (s6:167, s7:151) need no change: with the exception folded into (b), a compliant external-unavailable slice now SATISFIES (b) and is no longer \"absent\", so it correctly avoids the spurious routeBack:S4."
    },
    {
      "id": "XFC-2",
      "confirmed": false,
      "severity": "P3",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/surfaces/web-backend.md",
      "reasoning": "REJECT. The claimed terminology conflict between clause 6 (web-backend.md:111-115) and the two-axis `environment-scoped` model does not exist in the actual text; the finding's \"could read as conflicting\" depends on dropping two load-bearing qualifiers.\n\nClause 6 makes two propositions:\n(1) Requirement: \"the DESIGNATED dev DB/branch ... must be confirmed RUNNABLE and the behavior verified ON IT — the migration applied through the tool, the app boots, **the slice's path runs against that DB**.\" This is scoped to the DEV-env click-through activity (the \"browser/user-flow R2 -> DEV env\" axis). It never says the *automated-test* activity must run on the designated DB.\n(2) Prohibition: \"Verifying against the test/CI DB **while the designated DB stays broken** (un-migrated, drifted, or left mid-failure) is a FAIL.\" The conditioning clause \"while the designated DB stays broken\" makes this verbatim the `designated-env-broken` / `no_substitution` case (honest-verification.md:64 \"no switching to a working branch because the designated one is broken\"). It forbids using a test/CI-DB green as a substitute for an abandoned-broken designated DB — NOT running automated tests legitimately in the TEST env.\n\nThe finding's misreading requires ignoring both \"the slice's path runs against that DB\" (which scopes the requirement to the DEV/app-boots axis) and \"while the designated DB stays broken\" (which scopes the prohibition to substitution-under-breakage). A correctly-behaving verifier — designated DB left green AND automated tests run in TEST env with real fixtures — trips clause 6 in neither reading; the FAIL `migration: broke designated DB` only fires when the designated DB is actually left broken, which is genuinely a FAIL under both clause 6 and environment-scoped/no_substitution.\n\nDecisively, the pack's OWN verification paragraph already encodes the two axes without conflict in adjacent sentences (web-backend.md:196-201): it runs the migration test \"against a **representative copy** (real schema + sample rows)\" (the automated-test/TEST substrate) AND separately requires \"the **designated dev DB left green**\" (the DEV axis). The pack distinguishes the substrates itself; there is no instruction that the representative-copy test must execute on the designated DB. So clause 6 (DEV-axis app-boots + anti-substitution) and s5 §6.2 (activity-to-env mapping for both axes) are coherent.\n\nPer the honor-lean principle (teeth, not ceremony): the proposed parenthetical enumeration restates a distinction the existing two qualifiers already make. It is harmless and would be sandbox-valid (prose-only edit to a guardrail; failedCriterion strings are informational per runtime facts, and the proposed fix adds no field the engine reads), but it fixes no real misfire — the rule already has teeth exactly where it should and does not bite the legitimate two-axis flow. Not a real or material defect.",
      "refinedFix": "No fix warranted. Clause 6 is already coherent with the two-axis environment-scoped model: its requirement is scoped to the DEV-axis activity (\"the slice's path runs against that DB\" = app-boots/click-through) and its prohibition is scoped to substitution-under-breakage (\"while the designated DB stays broken\"). The pack's verification paragraph (web-backend.md:196-201) independently confirms the migration test runs against a \"representative copy (real schema + sample rows)\" while the \"designated dev DB left green\" — the two axes coexist without conflict. The proposed enumeration is ceremony, not teeth."
    },
    {
      "id": "XFC-3",
      "confirmed": false,
      "severity": "P3",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s6-verify-phase.md, /Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s7-verify-main-spec.md",
      "reasoning": "FACTUAL CORE VERIFIED, BUT IMMATERIAL — REJECT.\n\nWhat checks out: `no_substitution` appears ZERO times in s6-verify-phase.md and s7-verify-main-spec.md (confirmed by grep), while it is the named, deduped spine section in disciplines/honest-verification.md (line 38: \"DEDUPED HOME; other files reference this section\") and is composed explicitly by s5 §6.3 (line 220-233), s4, and s2. The hardening brief's own framing does assert s6/s7 should reference it: apply-hardening.mjs line 18 (\"other files REFERENCE it\") and the coherence-review acceptance criterion at line 62 (\"`no_substitution` is defined once in honest-verification.md and merely composed/referenced by s5/s6/s7\"). And the finding correctly concedes the SUBSTANCE is already covered in s6/s7.\n\nWhy it is NOT material (fails the teeth-not-ceremony bar):\n\n1. The fix changes nothing an agent does. s6 already refuses to credit `code-verified` as `verified` (lines 40-45, 135), rejects mock/stub stand-ins on the cross-slice path (lines 131-132), and demands DESIGNATED-env screenshots, flagging a convenient/wrong env as a tamper (lines 84-91, 165-166). s7 keeps verified/code-verified distinct (lines 103-109), rejects interims that cannot carry production load (lines 115-119), and demands designated-env evidence (lines 149-150). Adding a sentence that re-labels this existing behavior as \"this is `no_substitution`\" is exactly the restatement DESIGN §11 forbids (\"No duplication across files — reference a discipline module instead of restating it\"; \"If a sentence doesn't change what the agent does, delete it\").\n\n2. The drift concern the finding raises (\"a reader of s6/s7 alone never learns the principle has a deduped home\") is already mitigated. Both files extensively compose disciplines/honest-verification.md by name AT the exact subsections that hold the spine: s7 line 83-85 (\"Run the honest ladder to R4 (compose `honest-verification`)\" ... \"is the authority on what 'verified' may mean; do not restate it\"), s7 line 103 (\"`verified` vs `code-verified` — never conflate (from `honest-verification`)\"), s6 line 129 (tamper audit composed \"from `disciplines/honest-verification.md`\"). The reader is already pointed at the discipline as the authority with an explicit \"do not restate\" instruction — the deduped home is reachable; only the section-anchor label is absent.\n\n3. The proposed fix would ADD a new normative clause to s6/s7 — \"an unreconciled S4 obstacle-ledger entry is a `no_substitution` failure\" — but obstacle-ledger reconciliation is an S5-owned check (honest-verification.md lines 92-97; s5 §6.3 lines 226-229). s6 explicitly trusts S5 verdicts and must not redo them (s6 line 20). So the proposed text risks duplicating an S5 check at the phase/main-spec layer, mildly contradicting s6's own \"trust these S5 verdicts, you do not redo them.\"\n\nThe finding self-classifies as \"a mild composition gap, not a contradiction.\" Under the stated honor-lean (teeth, not ceremony) and DESIGN §11's leanness bar, a cosmetic section-anchor that alters no agent behavior and whose drift-prevention purpose is already served by existing file-level composition is ceremony, not a defect. Sandbox-validity of the proposed fix is not in question (reference-only, no new engine field) — but a sandbox-valid no-op is still a no-op.",
      "refinedFix": "None — reject. No edit warranted. If an editor nonetheless wants belt-and-suspenders drift protection without adding ceremony or duplicating S5's obstacle-ledger check, the minimal sandbox-valid form is a single anchor-only pointer (NOT a restated principle and NOT a new obstacle-ledger clause), e.g. in s6's \"Tamper / honesty audit\" section: \"(this seam-level audit is `disciplines/honest-verification.md` `no_substitution` at the integration scope — do not restate it)\" and a parallel parenthetical in s7 §4. But this is optional polish below the materiality bar, not a required fix."
    },
    {
      "id": "EFC-01",
      "confirmed": true,
      "severity": "P1",
      "file": "/Users/cracklehat/Sites/workflow-exploration/.claude/workflows/atlas.mjs",
      "reasoning": "CONFIRMED — both holes are real and material against the actual code/text.\n\nHOLE (a) ABSENT-LOG — REAL. atlas.mjs:424 reads refined-idea.md + idea-decisions.md only \"if the front door wrote them\" and \"(If absent, use the args context)\". The PREFLIGHT FLOOR at :425 keys its only trigger off \"if idea-decisions.md lists Detected Surfaces other than 'none' AND ...\". A direct Workflow-tool launch that bypasses the front door produces NO idea-decisions.md (and no refined-idea.md). The floor's condition is then vacuously false against a missing file, so S0 proceeds straight to \"Otherwise produce the durable main spec ... (verdict:PASS)\" at :426 and writes the spec for a money/auth/migration feature with zero rulings. The floor's own parenthetical says it exists to \"close a direct launch that bypassed the front door\" — yet that exact bypass is the one case it structurally cannot see, because it reads only a file the bypass omits and never consults args. The engine DOES carry the surface-naming material independently: lines 50-53 set initiative.idea=a.idea and initiative.decisions=a.decisions, and those are interpolated into the S0 task — so a fix can trigger off args even with the on-disk log absent. Nothing else in the S0 path catches this (line 429 only fires on an already-returned BLOCKED verdict).\n\nHOLE (b) SECTION-NAME — REAL. Verified against the templates: refine-idea/SKILL.md:51 puts `## Detected Surfaces (money|auth|data|migrations|external|PII|destructive — or \"none\")` in refined-idea.md. grill-idea/SKILL.md:71-84 lists idea-decisions.md's sections — `## Decision Depth, ## Decisions Resolved, ## Accepted Assumptions, ## Deferred Decisions, ## Surface Rulings, ## Required Capabilities & Preflight, ## Environments, ## Domain Language / Model, ## Success Checks, ## Questions Asked & Answered, ## Open / Blocking` — there is NO `## Detected Surfaces` section in idea-decisions.md; its surface section is `## Surface Rulings`. So the floor's primary trigger (\"if idea-decisions.md lists Detected Surfaces other than none\") is unevaluable against the file it names, and the cross-check degenerates to only the \"## Surface Rulings empty/placeholder\" half — which a non-empty placebo Surface Rulings defeats.\n\nMATERIALITY: Per the finding's framing, this S0 floor is the only engine-side teeth for failure (7) front-door-skipped on a direct launch. Both holes leave the one-way-door surfaces (money/auth/migration/etc.) — the very surfaces DESIGN §1.9/§12 say must be front-loaded conservatively — able to sail through to a written spec with no rulings. This is teeth-not-ceremony relevant: the floor as worded is largely ceremonial because its primary trigger names the wrong file and it is blind to the absent-log case it was built for.\n\nNOT a dead-vestige false positive: the fix consumes only sources the engine/agent already reads (refined-idea.md + idea-decisions.md at :424; args.idea/args.decisions in scope) and emits via the existing verdict:BLOCKED + failedCriterion envelope already handled at :429. No new launch-arg, no new return field. Pure prose rewrite of the task string — sandbox-valid (no FS/Node/Date/Math).",
      "refinedFix": "Replace the PREFLIGHT FLOOR sentence (atlas.mjs:425) with one that (1) sources the surface set from refined-idea.md `## Detected Surfaces` AND the args, (2) treats an absent/unparseable idea-decisions.md as itself BLOCKING when a domain surface is named, and (3) cross-checks against idea-decisions.md's ACTUAL sections. Exact replacement text for line 425:\n\n`PREFLIGHT FLOOR (closes a direct launch that bypassed the front door — this is the ONLY engine-side teeth for that bypass, so apply it even when no decision log exists): determine the DOMAIN-SURFACE SET = any of {money, auth, security, persistence/migration, external-effect, PII, destructive} that appears EITHER in refined-idea.md's \"## Detected Surfaces\" (if that file exists) OR named in the args idea/decisions text. (Note: \"## Detected Surfaces\" lives in refined-idea.md; idea-decisions.md instead carries \"## Surface Rulings\" + \"## Required Capabilities & Preflight\" — do NOT look for Detected Surfaces in idea-decisions.md.) Then STOP with verdict:BLOCKED (do NOT write the spec) if ANY of these hold: (i) the domain-surface set is non-empty AND idea-decisions.md is absent or unparseable — failedCriterion:\"front-door-skipped: no idea-decisions.md for detected domain surface <x>\"; (ii) a surface in the set has no resolved entry in idea-decisions.md's \"## Surface Rulings\" (empty/placeholder/unresolved counts as none) — failedCriterion naming the unruled surface; (iii) a domain/external surface has a required capability/config left un-provisioned AND un-waived (no waived-with-fallback) in \"## Required Capabilities & Preflight\" — failedCriterion naming the un-provisioned capability. The human resolves it in grill and re-launches.`\n\nThis routes through the existing engine handling: line 429 already records a BLOCKED s0 as a blocker and returns. No engine-side code change beyond this task string; no new fields read or returned. Sandbox-valid (pure prose; no FS/Node/Date.now/Math.random). Optionally also tighten line 424's \"(If absent, use the args context.)\" to \"(If absent, use the args context — but the PREFLIGHT FLOOR below still applies to surfaces named in the args.)\" so the agent does not read \"absent => proceed freely\"."
    },
    {
      "id": "EFC-02",
      "confirmed": true,
      "severity": "P2",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s7-verify-main-spec.md",
      "reasoning": "CONFIRMED as a real, material doc/vestige defect (not a false-green).\n\nEngine trace (atlas.mjs runInitiative, multi-phase S7, lines 462-474): verifyWithFix returns {status:'routeback', routeBack} when S7 emits FAIL+routeBack (line 209). Line 464 enters the routeback branch; line 465 logs it; line 466 re-enters ONLY when `final.routeBack.targetStep === 'S0'` (and budget remains). For targetStep S1/S2/S3/S4 the S0 test is false, so control falls to line 471: note(report.blockers, 'unhandled final-gate route-back to <ts>') and degrades. The phase is never re-run. report.ready is set true only at line 473 (final.status==='passed') or line 476 (single-phase); neither is reachable on the routeback path, and it defaults to false (line 82). So it FAILS CLOSED — correct, not a false-green.\n\nStep file mismatch: s7-verify-main-spec.md:151 promises `disciplines_attested` absence → `verdict:FAIL routeBack:S4`, and lines 158-161 (FAIL branch) say 'the engine re-enters the target and cascades downstream of it' for S4/S2/S1/S0. At S7 the engine re-enters ONLY S0; the S4 (and S1/S2/S3) route-back is silently dropped to a blocker. That is exactly DESIGN §11's dead-vestige smell: a module instructing the agent to emit a route the engine never honors at this step.\n\nAsymmetry confirmed: the phase loop at line 448 DOES re-run the owning phase on an S1-S4 route-back (the S6 path, via line 405 escalate → loop). S6's S4 route-back re-runs; S7's does not. Materiality: P2 — it cannot green a bad ship (fail-closed), but it misleads the verifier agent and the operator about what S7 does with a build-discipline gap. The doc and engine must agree.\n\nFix selection (teeth, not ceremony): option (a) — teach the engine to re-run phases on a non-S0 S7 route-back — is a larger, riskier change to the terminal gate (S7's only post-gate re-entry primitive is the full runInitiative restart at line 446; there is no 'resume at phase X then re-run S7'), and S7's BLOCKED branch is already degrade-only by design (lines 162-165, DESIGN §1.10). The consistent, lean, sandbox-valid fix is option (b): correct the step file so the documented route matches the degrade-only engine, killing the vestige. The doc may keep S0 as a real route-back (the engine honors it) but must stop promising S1-S4 re-entry/cascade at S7.",
      "refinedFix": "Edit s7-verify-main-spec.md so the documented route matches the engine's actual S7 behavior (degrade-only for non-S0; only S0 is re-entered). No engine change; sandbox-valid (pure text).\n\n1) Replace line 151 ('Any of (a)/(b)/(c) absent → `verdict:FAIL` `routeBack:S4` (build-discipline gap); ...') with:\n\n  Any of (a)/(b)/(c) **absent** → `verdict:FAIL` `failedCriterion:disciplines_attested` (build-discipline gap). **At S7 this does NOT trigger a rebuild:** the engine re-enters only an **S0** final-gate route-back; an S4/S1/S2/S3 route-back here is recorded as a blocker and the run **degrades** (`ready` stays false). Fix the offending slice on disk and **re-launch** — S7 is the terminal gate, not a rebuild loop. An artifact that exists but is **unreadable** → `BLOCKED`.\n\n2) Replace the FAIL bullet (lines 156-161) with one that drops the false S1-S4 re-entry/cascade promise:\n\n- **`FAIL`** — a criterion is observably wrong, a seam does not compose, or the spot-checked slice's build disciplines are not honored on disk (`disciplines_attested`), and the fix is a build change. Set `failedCriterion` to the named gate criterion above. **Only an `S0` route-back is re-entered at the final gate** (spec/phase-map itself is wrong); emit `routeBack:{targetStep:'S0', ...}` for that case and the engine re-runs S0 and cascades (bounded by `maxUpstreamRouteBacks`). For a **slice/build-level** gap (the `disciplines_attested` case), emit `verdict:FAIL` with `failedCriterion` and **no actionable rebuild route** — the engine degrades (records the blocker, `ready` stays false); re-launch after fixing the slice. (S7 itself never edits code.)\n\nThis keeps S0 as the one honest S7 route-back (engine line 466 honors it), removes the vestigial S1-S4 cascade promise the engine drops, and matches the existing degrade-only BLOCKED posture two bullets down — the documented route and the engine now agree."
    },
    {
      "id": "EFC-03",
      "confirmed": true,
      "severity": "P2",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s5-verify-build.md",
      "reasoning": "CONFIRMED — real and material. Verified every premise against the actual code/text:\n\n1. Hard FAIL path is real (teeth): s5 §6.1 lines 192-199 + s4 criterion 7 lines 168-178 make an absent/failing/unwritten behavior test a `behavior-test-present`/`behavior-test-committed` FAIL with routeBack:null into the bounded fix loop. Solid for the common case.\n\n2. The escape exists: s5 lines 195-199 and honest-verification.md line 83 convert the owed FAIL into a committed `.skip`-with-reason + verdict:BLOCKED/code-verified when the agent classifies the slice `external-genuinely-unavailable`.\n\n3. The classification is engine-invisible: grep of atlas.mjs for classification/external-genuinely-unavailable/verifiedKind/code-verified/ladderReached returns NONE. The engine routes only on verdict (PASS/FAIL/BLOCKED) + routeBack + structured per-step fields. The classification is pure prose the agent self-applies — consistent with RUNTIME FACT (2) and DESIGN §11.\n\n4. Degrades safely (the finding's own caveat holds): a BLOCKED slice never marks the phase `verified`, report.ready stays false — so a wrongly-claimed external-unavailable does not silently ship a green tick. But it DOES let the slice escape the re-build loop with a skipped test.\n\n5. The gap is real: the ONLY corroboration the current text demands (s5 lines 260-261 \"independently corroborate the harness is GENUINELY unrunnable with the exact missing capability named\"; honest-verification line 83 \"tried .env.example, sandbox, test key, local fake\") is a generic self-checklist. It NEVER cross-references idea-decisions.md `## Required Capabilities & Preflight`, even though the grill SKILL.md (lines 78, 86-91) authoritatively records, per capability, `provisioned?` (yes|no|waived-with-fallback) AND a concrete `how-to-verify-present` probe the build can run. S0's preflight floor (mjs lines 425, engine 429) even blocks launch when a domain/external capability is left un-provisioned and un-waived — so at build time every dependency is provisioned-with-probe, waived-with-fallback, or probe-less. A slice claiming a `provisioned?=yes`-with-probe capability is unreachable is making a claim the front-door ledger directly contradicts, and nothing forces that confrontation. Verifier self-attestation alone grounds the escape => self-declared-unreachable dodge to avoid writing the real test. Material because it punctures the one teeth-bearing gate (the behavior-test FAIL loop) for slices touching exactly the domain/external surfaces v2 treats as one-way/CRITICAL.\n\nThe proposed fix is sound and sandbox-valid; I refined and applied it.",
      "refinedFix": "Applied to s5-verify-build.md §6.3 (after the obstacle-ledger reconciliation, before the verifiedKind reporting). Inserted a corroboration requirement anchored to the ACTUAL front-door field names: before a slice may take the BLOCKED/code-verified escape, cross-check the claimed-unreachable capability against idea-decisions.md `## Required Capabilities & Preflight` (capability | why | `provisioned?` | `how-to-verify-present`). If it is `provisioned?=yes` OR carries a `how-to-verify-present` probe, the verifier MUST run the probe; a passing probe => claim is false => `behavior-test-present` FAIL (verdict:FAIL, routeBack:null — owed real test), not BLOCKED. Only a capability marked un-provisioned / waived-with-fallback, or one with no probe, may ground the BLOCKED escape (and only after independent confirmation per §6.1). Sandbox-valid: pure agent-prose instruction to read an already-granted on-disk file (idea-decisions.md — already referenced by S5 §6.2 and honest-verification.md) and run a probe; no new launch-arg, no new return field, no fs/Date/Math API. Engine routing is unchanged — it already re-enters the fix loop on verdict:FAIL/routeBack:null, so the closed dodge needs zero engine change, preserving the no-loop property (a genuinely un-provisioned/waived/probe-less path still BLOCKs and never re-builds). Honors lean: ~9 lines, all teeth, reuses existing field names and the existing FAIL routing rather than adding ceremony. Optional reinforcement (not applied, to avoid duplication): mirror one clause into honest-verification.md line 83's `external-genuinely-unavailable` table row so the deduped home also names the front-door probe as the corroboration source."
    },
    {
      "id": "EFC-04",
      "confirmed": true,
      "severity": "P3",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s5-verify-build.md",
      "reasoning": "Verified against the actual text. s5-verify-build.md §6.2 (lines 201-218) scopes every verification activity to \"the environment NAMED in idea-decisions.md '## Environments'\" and frames the whole gate around the DEV-click-through-vs-TEST-tests split (lines 203-208). grill-idea/SKILL.md line 98 confirms the finding's premise: '## Environments' is MANDATORY only 'whenever a persistence/migration/external/UI surface is present; otherwise state the repo default.' So for a STANDARD non-domain slice with no such surface, '## Environments' can legitimately be a bare 'repo default' line with no DEV/TEST split — and §6.2's reference point ('proper environment') is then under-determined.\n\nThe gap is real but narrow. §6.2 already names 'arbitrarily switching envs for convenience' as an automatic environment-scoped FAIL (line 212), and honest-verification.md repeats this (lines 64, 181). However, that existing trigger is a MOTIVE judgment ('for convenience') the verifier must infer — soft, not diff-checkable. The finding correctly spots that in the bare-repo-default case there is no named anchor to make a mid-slice switch falsifiable, which is exactly the STANDARD/non-domain class where env-switching-to-get-green hides. The proposed fix converts the soft motive predicate into a mechanical consistency invariant — and §6.2 ALREADY requires a per-activity env stamp (base URL/build SHA/data tier/simulator target) recorded in the evidence table (lines 174, 209-210), so the invariant is checkable from artifacts the verifier already produces. This adds teeth, not ceremony.\n\nSandbox-validity / dead-vestige check passes: the fix is pure prose tightening of an EXISTING verdict-bearing gate (environment-scoped, a PASS criterion at §6 line 174, routed via verdict:FAIL). The engine (atlas.mjs) routes on verdict + routeBack/fixNeeded (lines 196-236) and treats failedCriterion as an informational label only (lines 211, 220, 232) — so the fix introduces NO new launch-arg, NO new return field, and instructs no field the engine never reads. It is not a DEAD VESTIGE per DESIGN §11. Confirmed.",
      "refinedFix": "In s5-verify-build.md §6.2, after the existing 'arbitrarily switching envs for convenience ... is a tamper' sentence (line 213), add:\n\n\"When idea-decisions.md `## Environments` names only a **repo default** (no DEV/TEST split — permitted for a STANDARD non-domain slice), `environment-scoped` binds to a **consistency invariant**: resolve that single default once, and **every** activity's per-activity stamp (base URL / host / build SHA / data tier / simulator target — already required in the evidence table) must show the **SAME** resolved environment. Any change of base URL/host/data tier BETWEEN activities within the slice, absent an explicit recorded reason, is a wrong-env tamper → automatic `environment-scoped` FAIL (`routeBack: null`, `fixNeeded` names the activity that switched). This gives the gate a falsifiable invariant even without a named DEV/TEST split.\""
    },
    {
      "id": "EFC-05",
      "confirmed": true,
      "severity": "P3",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s5-verify-build.md",
      "reasoning": "Verified against actual code/text. The structural premise is real: (1) follow-the-house-pattern branch (b) \"divergence-justified\" is self-graded by S2 (s2-plan.md:106-108, 218-225); (2) on a FAST-lane STANDARD slice S2's plan is the only plan-time gate (s2-plan.md:13-15) because S3 runs FULL-lane only (engine runSlice line 310: `if (lane === 'FULL')`). So at PLAN time there is no second party on the reinvention-vs-reuse call for FAST slices. The teeth the finding credits are accurate: sibling-surveyed blocks grounded/S2 (read-before-write.md:127-133), and follow-the-house-pattern is a hard reuse-checked FAIL with routeBack:null repaired by re-planning onto the sibling (s2-plan.md:218-225) — both engine-routable via plan.verdict + structured fields, no dead vestige.\n\nHOWEVER the finding OVERSTATES the gap. It claims \"the divergence-justified branch is not independently checked on FAST.\" It IS, partially: S5 — a separate non-implementer verifier on BOTH lanes (s5-verify-build.md:1-11, 178-180) — already reconciles S4's build.md grounding note (which inlines the SIBLING:/SIBLING PATTERN: lines per s4-build.md:21,117) against what it observed, and a hand-rolled reimplementation of an existing sibling is ALREADY a no_substitution/tamper FAIL (honest-verification.md:48-50 \"a hand-rolled reimplementation... is a TAMPER, not a pass\"; tamper checklist :174-175 \"Workaround-to-green... hand-rolled reimplementation... no_substitution FAIL\"; s5-verify-build.md:228-229 \"any prerequisite S4 recorded as substituted, side-stepped, or hand-applied is a no_substitution FAIL\"). So a second party (S5) already owns the FAIL that an unsubstantiated branch-(b) divergence falls under.\n\nWhat is genuinely missing — and what makes this a real-but-P3 sharpening rather than a missing gate — is the EXPLICIT naming of the branch-(b) case in S5's §6.3 reconcile list. Current no_substitution text targets substituted/hand-applied prerequisites and generic hand-rolled reimplementation; it does not explicitly instruct the verifier to re-grade a SIBLING: hit where the plan chose branch (b) and confirm the divergence is a concrete contract/lifecycle/shape difference, not a preference. Naming it converts an implicit catch into a diff-checkable named reconcile item — teeth, not ceremony. Sandbox-valid: reads only build.md grounding note + plan.md (already read on-disk) and the existing envelope; adds no launch-arg, no new return field; rides the existing no_substitution FAIL path + failedCriterion. Confirmed at P3 with a refined fix that hooks the EXISTING §6.3 reconcile (not a brand-new check, which would wrongly imply S5 didn't already own the reuse call and would inflate ceremony).",
      "refinedFix": "Sharpen the EXISTING no_substitution reconcile in s5-verify-build.md §6.3 (do not add a new criterion). In the paragraph at lines 226-229, after \"...is a no_substitution FAIL (routeBack: null if in-footprint; routeBack upstream if the missing prerequisite should have been front-loaded at the front door).\", append:\n\n\"**Divergence-reconcile (the FAST-lane reuse second-party).** When the grounding note records a `SIBLING:` hit AND the plan took branch (b) divergence-justified (`SIBLING PATTERN: ... | divergence justified: <arg>`), re-grade that argument yourself: it PASSES only if the divergence names a concrete **contract / lifecycle / shape** difference the sibling genuinely cannot carry. A preference ('cleaner contract', 'rather write it fresh') over a reinvented sibling is **not** a divergence — the fresh code is a hand-rolled reimplementation → `no_substitution` FAIL (`routeBack: null`; `fixNeeded`: re-plan onto the sibling). This puts the non-implementer on the reinvention-vs-reuse call on the FAST lane, where S3 never ran — without adding a step or a field.\"\n\nThis is sandbox-valid: S5 already reads build.md's grounding note and plan.md; the SIBLING:/SIBLING PATTERN: lines are on-disk (s4-build.md:21,117 / s2-plan.md:113); the verdict rides the existing no_substitution failedCriterion the engine already routes on. No new launch-arg, no new return field — honors DESIGN §11 (no dead vestige) and the lean bar."
    },
    {
      "id": "LNC-01",
      "confirmed": true,
      "severity": "P1",
      "file": "/Users/cracklehat/Sites/workflow-exploration/.claude/workflows/atlas.mjs",
      "reasoning": "CONFIRMED — real and material. Verified all three factual premises against the actual files:\n\n1. atlas.mjs:425 (PREFLIGHT FLOOR), first AND-conjunct reads: \"if idea-decisions.md lists Detected Surfaces other than \\\"none\\\" AND its \\\"## Surface Rulings\\\" is empty/placeholder/unresolved\". The possessive \"its\" scopes BOTH conjuncts to idea-decisions.md, so the floor claims \"Detected Surfaces\" is an idea-decisions.md field.\n\n2. grill-idea/SKILL.md:71-84 — the FULL idea-decisions.md template has NO \"## Detected Surfaces\" heading. Its surface-related fields are \"## Surface Rulings\" (line 77) and \"## Required Capabilities & Preflight\" (line 78). Confirmed by grep: the only occurrences of \"Detected Surfaces\" in grill's SKILL.md are at lines 119/122 (the gate prose), never in the template itself.\n\n3. refine-idea/SKILL.md:51 — \"## Detected Surfaces (money | auth | data | ... — or \\\"none\\\")\" lives in the refined-idea.md template. So Detected Surfaces is refine's output field, not grill's.\n\nTherefore the floor's first conjunct keys on a field that does not exist in the file it names. The S0 agent reading idea-decisions.md literally finds no Detected-Surfaces list, yielding two bad outcomes: (a) treat absent-as-\"none\" so the floor NEVER fires and the direct-launch bypass it was added to close sails through with an empty Surface Rulings; or (b) silently cross-jump to refined-idea.md, which the conjunct grammar did not direct. Either way the floor's primary AND-conjunct is unreliable, defeating its purpose. This violates DESIGN §11 (\"Every gate states its PASS condition as a checkable, named-criterion verdict\").\n\nMateriality: this is the bypass-closer added by the hardening; a floor that can silently no-fire on the exact bypass path is a teeth-vs-ceremony failure, not cosmetic. Sandbox-valid to fix: line 424 already names BOTH refined-idea.md and idea-decisions.md as read at S0, so re-anchoring to the genuine cross-file pair adds no new launch-arg or return field — on-disk prose only.\n\nSame mis-attribution exists in grill-idea/SKILL.md:121-122 (tests \"## Surface Rulings ... while Detected Surfaces is non-none\" without naming the file); lower-stakes there because the grill agent holds both files in live context, but it should be mirrored for consistency.",
      "refinedFix": "Re-anchor the floor's first conjunct to the file that actually carries each field; both files are already read at line 424, so this is sandbox-valid (on-disk prose only).\n\nIn atlas.mjs:425, replace the trigger with:\n\"PREFLIGHT FLOOR (closes a direct launch that bypassed the front door): if refined-idea.md's \\\"## Detected Surfaces\\\" is non-\\\"none\\\" AND idea-decisions.md's \\\"## Surface Rulings\\\" is empty/placeholder/unresolved (any detected surface lacks a locked rule), OR a domain/external surface has a required capability/config left un-provisioned and un-waived in idea-decisions.md's \\\"## Required Capabilities & Preflight\\\" — STOP: return verdict:BLOCKED with failedCriterion naming the unruled surface / un-provisioned capability, and do NOT write the spec. The human resolves it in grill and re-launches.\"\n\nKey change: the Detected-Surfaces conjunct now explicitly reads refined-idea.md (where that field lives), the Surface-Rulings conjunct reads idea-decisions.md, making both conjuncts checkable against named files.\n\nMirror in grill-idea/SKILL.md:121-122 so the gate and the engine floor name identical fields: change \"An empty or placeholder \\\"## Surface Rulings\\\" while \\\"Detected Surfaces\\\" is non-\\\"none\\\"\" to \"An empty or placeholder idea-decisions.md \\\"## Surface Rulings\\\" while refined-idea.md's \\\"## Detected Surfaces\\\" is non-\\\"none\\\"\"."
    },
    {
      "id": "LNC-02",
      "confirmed": true,
      "severity": "P2",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s5-verify-build.md",
      "reasoning": "CONFIRMED — real, material, prose-only seam between two hardened modules.\n\nVerified against actual text:\n- s5-verify-build.md §6 criterion #7 `environment-scoped` (lines 173-174) is an UNCONDITIONAL S5 PASS gate, and lines 178-179 state criteria 6-8 are owned by the verifier \"on every slice in both lanes.\" So it applies to a pure STANDARD CLI slice with no UI/domain surface.\n- §6.2 (line 203) scopes each activity \"to the environment named in idea-decisions.md '## Environments'\" and requires it be RECORDED per-activity — controlling sentence presupposes that section exists and names an env. The mirror in honest-verification.md `no_substitution` R2-gate (line 54) says the same: \"in the PROPER environment for the activity named in idea-decisions.md '## Environments'.\"\n- grill-idea/SKILL.md lines 97-98 confirm the premise exactly: `## Environments` is \"MANDATORY whenever a persistence/migration/external/UI surface is present; OTHERWISE state the repo default.\" So for a non-UI/non-domain STANDARD CLI/library slice, grill legitimately writes NO DEV/TEST split — only a repo default.\n- grep confirms NO existing graceful-path language for \"no Environments section / single repo-default env\" anywhere in s5 or honest-verification (only an unrelated \"absent behavior test\" precedence on s5:192).\n\nWhy material (not merely theoretical): honest-verification lines 64-68 escalate behavior \"in the wrong environment for the activity\" to \"code-verified AT BEST → BLOCKED, never verified\" for STANDARD/CRITICAL, and the tamper checklist (lines 179-181) is written entirely around a DEV/TEST split. A literal-minded verifier who cannot find a named env in an absent `## Environments` has a documented hook to treat the unrecorded/unscoped env as wrong-env and FAIL/BLOCK an honest CLI slice that simply ran `node bin/cli.js` against the repo default. The system elsewhere supplies exactly this kind of explicit escape (the BEHAVIOR-TEST PRECEDENCE on s5:192-199; the missed-surface 'safety net, not a failure' on s5:40) — its absence here is a genuine asymmetry, not ceremony.\n\nSandbox-validity: fix is on-disk prose only; the engine reads verdict/failedCriterion/routeBack and never any new field, so adding a sentence to two .md modules changes nothing the engine reads — DESIGN-§11/RUNTIME-FACT compliant. It also does NOT instruct the agent to emit any unread field (it constrains how the existing `environment-scoped` criterion is judged), so it is not a dead vestige.\n\nThe finding's line citations (s5:201-213, 173-174; grill:92-98) and its observation that the DEV-browser clause is correctly conditional (so no false browser is imposed — the hole is purely the missing single-default escape) are all accurate.\n\nDefaulting to confirmed=true is warranted: the defect is REAL (a literal verifier can false-FAIL an honest slice) and the fix carries teeth (it preserves the action-based tamper FAILs — switching/mutating away from the default is still a FAIL — while closing the false-positive hole).",
      "refinedFix": "Two-site, prose-only, sandbox-valid mirror edit.\n\nSITE 1 — /Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s5-verify-build.md, §6.2, insert immediately after line 203 (the \"Each verification activity is scoped to the environment named in idea-decisions.md '## Environments'\" sentence), as a new bullet/paragraph:\n\n\"**No declared split ⇒ the single repo default is the proper env (not a tamper).** When idea-decisions.md declares no DEV/TEST split — a legitimate omission for a non-UI, non-domain slice ('otherwise state the repo default', grill-idea §Environments) — there is ONE repo-default environment. Record THAT env per activity and pass; a missing `## Environments` section is not itself an unrecorded-env or wrong-env tamper. `environment-scoped` FAILs only on an observed switch/mutation AWAY from the declared-or-default env (the action-based tampers below), never on the mere absence of a split.\"\n\nSITE 2 — /Users/cracklehat/Sites/workflow-exploration/atlas-engine/disciplines/honest-verification.md, in the `no_substitution` (R2 gate) block, append after the two DEV/TEST bullets (after line 60):\n\n\"- **No declared split** — when `## Environments` declares no DEV/TEST split (legitimate for a non-UI/non-domain slice; grill states the repo default), the single repo-default env IS the proper environment for every activity: record it and proceed. A missing split is not a wrong-env substitution; the wrong-env tamper requires an observed switch/mutation away from the declared-or-default env.\"\n\nBoth insertions preserve the existing action-based FAIL teeth (lines 211-213 / lines 64-68) verbatim and only add the absent single-default escape."
    },
    {
      "id": "LNC-03",
      "confirmed": false,
      "severity": "P3",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s4-build.md",
      "reasoning": "REJECT — real-ish concern, not material; mischaracterizes a surface-triggered gate as flat ceremony, and proposes a behaviorally-null fix.\n\nVerified against actual files: s4-build.md:136-188 (8 named `built` criteria + the 186-188 Note) and s5-verify-build.md:164-176 (8 PASS conditions) exist as the finding cites. DESIGN §1.8 (ceremony-on-everything trains skimming) and §11 (no dead vestiges / lean = effective) are real and quoted faithfully. The engine (.claude/workflows/atlas.mjs:118-139, SCHEMA_S4) reads only verdict/failedCriterion/built/riskTier/splitNeeded/replacementSlices + envelope — the named-criteria strings are informational, matching the runtime facts. So the textual premises check out; the defect classification does not.\n\nThree reasons it fails the \"real and material\" bar:\n\n1. The \"~16 equally-weighted gates for a small unit of logic\" premise conflates LISTED with ENGAGED criteria. Atlas v2 is surface-triggered by design (DESIGN §1.8, §4). For a genuine small STANDARD non-domain slice, criterion 4 (guardrails-attached) is empty (no fired surface), 5 (obstruction-resolved) is vacuous (no obstruction), 6 (no-silent-workaround) never engages (no external capability required), and 8 (declare-new-runtime-config) is vacuous (no new env read). What actually bites such a slice is ~1-3 (compiles/maps/two-hats) + 7 (behavior-test). That is the design's ANSWER to §1.8's ceremony failure mode, not an instance of it.\n\n2. The Note at s4-build.md:186-188 that the finding flags as skim-inducing prose is itself load-bearing anti-gaming text: it closes the exploit where a silent workaround \"maps cleanly\" to every-change-maps (crit 2) and thus looks green. It is the single sentence keeping crit 2 from being a fig leaf over crit 6. The finding's \"long prose bodies push toward skim\" framing undersells this.\n\n3. The spine criteria are ALREADY the most emphasized material in the gate: crit 6 carries \"tamper, not a pass\", crit 7 carries \"Choosing not to write it because it would fail is exactly the failure this blocks\", and the Note already labels 6-8 \"independent teeth.\" The proposed bolded-subheading fix is cosmetic, behaviorally null, and substantially already present. Worse, the optional \"fold criterion 8 under criterion 6\" sub-proposal blurs two distinct named criteria that produce different failedCriterion strings (capability-unavailable BLOCK vs config-unregistered FAIL) — a real semantic smudge contradicting the \"no semantic change\" claim. The finding self-rates P3, \"a real but bounded smell, not a blocker,\" \"honest and lean-by-intent, no gate removed\" — i.e. by its own admission immaterial; and §11's lean bar cuts AGAINST adding presentation scaffolding whose effect on agent behavior is unproven. Sandbox validity is moot since no defect is confirmed."
    },
    {
      "id": "ASF-1",
      "confirmed": true,
      "severity": "P1",
      "file": "/Users/cracklehat/Sites/workflow-exploration/.claude/workflows/atlas.mjs",
      "reasoning": "CONFIRMED — real and material. Verified every load-bearing claim against the actual code/text:\n\nMECHANICAL PATH (confirmed): An S4 front-door-prereq Tier-3 obstruction emits routeBack:S0 (s4-build.md §6 line 167; obstruction-loop.md §4 lines 186-188, 205, 223 explicitly instruct \"routeBack:S0 ... so grill names it on re-launch\"). In the engine: routeSplitOrObstruction (lines 274-276) returns {result:'escalate'} for tier===3 with a non-S2/S3 target; runSlice returns it (lines 325-328); runPhase returns {escalate} (lines 394-398); runInitiative's escalate loop hits line 443: `if (ts === 'S0' && upstreamRouteBacks < cfg.maxUpstreamRouteBacks) { upstreamRouteBacks++; return await runInitiative() }`. There is NO branch anywhere that distinguishes a front-door-owed-prereq S0 route-back from a spec-shape S0 route-back — lines 441-457 and 464-471 (S7) are the only S0-escalation sites and both treat all S0 route-backs as identical in-run S0 re-entries.\n\nNO-OP LOOP (confirmed by grep): The engine only READS idea-decisions.md (line 424) and NEVER writes it. S0 writes only main-spec.md / development-plan.md / acceptance-criteria.md (line 426). So the runtime-capability condition that triggered the route-back is provably unchanged between S0 re-entries — re-running S0 against the identical, unedited idea-decisions.md cannot change anything.\n\nOUTCOME (b) (confirmed material): If the capability IS listed/waived in `## Required Capabilities & Preflight` but fails only at RUNTIME (exactly what S4's capability-unavailable catches), the document-only PREFLIGHT FLOOR (lines 425/429) sees nothing wrong → S0 returns PASS → engine resumes the identical build → S4 hits the same runtime-missing capability → routeBack:S0 again. With maxUpstreamRouteBacks=2 (line 36) the guard `upstreamRouteBacks < 2` permits exactly two full S0+build no-op cycles (0<2 true, 1<2 true, 2<2 false) before falling through to the GENERIC line-454 blocker (\"unhandled route-back to S0 ...\"). The budget a genuine spec-shape S0 route-back would need is consumed by a loop that cannot change anything, and the route-back's stated purpose (\"grill names it on re-launch\") is never achievable in-run because a workflow cannot pause for the interactive front-door/grill layer (DESIGN §12). Outcome (a) — genuinely-absent capability → PREFLIGHT FLOOR fires on re-run → clean BLOCKED degrade — is real but only wastes one round-trip; (b) is the material harm.\n\nSANDBOX-VALIDITY of the fix (confirmed): the guard reads only r.escalate.check / r.escalate.reason, both in the ROUTEBACK schema the engine already reads (lines 113-117, used by logRouteBack line 98); uses a pure-JS regex; calls existing note()/break. No filesystem/Node/Date/Math API. Valid.\n\nONE CAVEAT on the proposed regex: the free-text match includes broad tokens (auth, tool, secret) and would false-positive on a legitimate spec-shape S0 route-back whose reason merely mentions e.g. \"auth\" — incorrectly terminating a productive S0 re-entry. The failure mode of a false positive is benign (degrade with a named, actionable blocker rather than a no-op loop), so it is tolerable, but the refined fix below prefers an exact canonical check token first and only falls back to the (tightened) regex, and factors the guard into one helper applied at BOTH the initiative-loop S0 branch (line 443) and the S7 final-gate S0 branch (line 466) so they stay identical. The finding correctly identifies both sites and the right structural-field-only approach given that r.obstruction is not available at the escalate-loop layer (only r.escalate/the routeBack is).",
      "refinedFix": "Add a single helper above runInitiative (sandbox-valid: reads only routeBack.check/reason; pure JS):\n\n```js\n// A front-door-owed prerequisite that fails only at RUNTIME (auth/tool/secret absent) cannot be\n// fixed by re-running S0 against the unchanged idea-decisions.md — the engine never writes that file,\n// so an S0 re-entry is a guaranteed no-op that burns maxUpstreamRouteBacks. Degrade NOW with a named,\n// actionable blocker the human resolves in grill before re-launching (DESIGN §12: a workflow cannot pause).\nconst isFrontDoorPrereqS0 = (rb) => {\n  if (!rb || rb.targetStep !== 'S0') return false\n  const c = (rb.check || '').toLowerCase()\n  if (c === 'front-door-prereq' || c === 'capability-unavailable') return true   // canonical token (preferred)\n  if (c === 'one-way-obstruction') return false                                   // engine-reclassified spec route-back\n  return /capability|prereq|provision|front-load|credential|secret|env\\s*var|capability-unavailable/i\n    .test((rb.check || '') + ' ' + (rb.reason || ''))\n}\nconst blockFrontDoorPrereq = (where, rb) => note(report.blockers, { where,\n  reason: 'front-door prerequisite missing/failing at RUNTIME — re-run grill to provision it, then re-launch: ' + (rb.reason || rb.check || '') })\n```\n\nThen in runInitiative's escalate loop, BEFORE the `ts === 'S0'` re-entry branch (currently line 443):\n\n```js\nif (isFrontDoorPrereqS0(r.escalate)) { blockFrontDoorPrereq('S0', r.escalate); break }\nif (ts === 'S0' && upstreamRouteBacks < cfg.maxUpstreamRouteBacks) { ... }   // unchanged\n```\n\nAnd mirror it in the S7 final-gate S0 branch (currently lines 465-466), BEFORE the upstreamRouteBacks re-entry:\n\n```js\nlogRouteBack('S7', final.routeBack)\nif (isFrontDoorPrereqS0(final.routeBack)) { blockFrontDoorPrereq('S7', final.routeBack) }\nelse if (final.routeBack.targetStep === 'S0' && upstreamRouteBacks < cfg.maxUpstreamRouteBacks) { ... }   // unchanged\n```\n\nWhy tightened vs the finding's regex: dropped the bare `tool`/`auth` alternatives (high false-positive rate against legitimate spec-shape S0 route-backs that merely mention auth) and added an exact-`check` fast path. Optional but recommended companion edit (NOT required for the engine fix to work): have s4-build.md §6 / obstruction-loop.md §4 emit `check:\"front-door-prereq\"` on this specific routeBack so the engine matches the canonical token instead of relying on the prose-heuristic fallback — that turns a heuristic into a clean structured-field route and is the durable home for the discrimination."
    },
    {
      "id": "ASF-2",
      "confirmed": true,
      "severity": "P1",
      "file": "/Users/cracklehat/Sites/workflow-exploration/.claude/workflows/atlas.mjs",
      "reasoning": "CONFIRMED — real and material. Verified against the actual engine text (atlas.mjs:424-429), the S0 module (steps/s0-spec-and-phases.md), DESIGN.md (§11 dead-vestige, §12 two-layer front door), and both front-door SKILLs.\n\nThe defect, point by point:\n1. The PREFLIGHT FLOOR (line 425) headline claims it \"closes a direct launch that bypassed the front door,\" but every clause of its condition reads CONTENT that lives only inside the front-door artifacts: branch 1 keys on \"Detected Surfaces other than 'none'\" (which actually lives in refined-idea.md per refine SKILL line 51) AND an empty \"## Surface Rulings\" (in idea-decisions.md); branch 2 keys on a capability in \"## Required Capabilities & Preflight\" (idea-decisions.md). When BOTH front-door files are absent — the full-bypass case — none of those lines/sections exist, so both OR-branches are vacuously false and the floor never fires. The floor only catches the PARTIAL bypass (file present, rulings empty), exactly as the finding states.\n\n2. The full-bypass path is real and engine-anticipated, not a strawman: line 424 explicitly says \"if the front door wrote them\" / \"(If absent, use the args context),\" and line 55 only logs a warning on missing `idea` then proceeds. grill SKILL Handoff and front-door/atlas.md step 3 both describe the supported invocation as \"call the Workflow tool with scriptPath = .claude/workflows/atlas.mjs\" — so invoking the Workflow tool directly with raw args, skipping refine/grill, is a reachable supported path with zero file-presence gate (grep confirms 424-429 is the ONLY front-door reference in the engine).\n\n3. With both files absent, S0 falls back to args + its own §0 coarse self-detection (s0 module §24-39), which is explicitly \"a hint, not a verdict\" used only to make the spec modality-correct; the S0 gate §6 has NO front-door-presence criterion and no instruction to BLOCK when a one-way surface is detected without a ruling. So a full-bypass launch over a money/auth/migration/external/PII/destructive idea writes the spec with zero user surface rulings and zero provisioned capabilities — precisely the autonomous-safety one-way-door hole the floor was added to close (DESIGN §1.9, §12: money/migrations/auth/external are one-way by rule; grill must front-load them).\n\n4. The fix is sandbox-valid and lean (teeth, not ceremony): it is prose-only appended to the floor; the S0 agent already does the on-disk read (line 424) and already runs §0 detection, so no FS/Node API is needed in the engine. It reuses verdict:BLOCKED + failedCriterion — both already read by the engine at line 429 (which routes solely on verdict and treats failedCriterion as informational, matching RUNTIME FACTS). It introduces no new launch-arg and no new return field, so it is not a DEAD VESTIGE (DESIGN §11). The failedCriterion string also matches the module's \"criterion: detail\" grammar (s0 module §6).\n\nThis closes the FULL-bypass path the floor's own headline claims to close; the existing floor only ever covered the partial-bypass.",
      "refinedFix": "Append to the PREFLIGHT FLOOR (end of atlas.mjs line 425), making artifact-absence itself a trigger and anchoring it to S0's existing §0 coarse detection so it stays sandbox-valid (prose-only; the S0 agent does the on-disk read it already performs at line 424):\n\n\" FULL-BYPASS CASE: if BOTH idea-decisions.md AND refined-idea.md are ABSENT (a direct launch that never ran the front door), run your §0 coarse surface detection over the args; if it fires ANY one-way/domain surface (money | auth | persistence-migration | external-effect | PII | destructive), STOP the same way — return verdict:BLOCKED with failedCriterion:\\\"front-door-bypassed: <surfaces> detected with no idea-decisions.md ruling\\\" and do NOT write the spec; the human must run refine→grill and re-launch. A launch whose §0 detection finds NO one-way/domain surface may proceed from args alone.\"\n\nRationale for the tightening vs. the proposed text: (a) explicitly ties the trigger to the §0 detection the S0 module already runs (s0 module §24-39) rather than introducing a fresh \"coarse §0 detection\" notion, keeping it a reference not a restatement (DESIGN §11 no-duplication); (b) keys the absence check on BOTH artifacts being absent so it cannot collide with the existing partial-bypass clause (which already handles the file-present cases); (c) the failedCriterion follows the module's \"<criterion>: <named offender>\" grammar (s0 module §6, §209). The engine needs NO change — line 429 already routes on verdict:BLOCKED and records failedCriterion. Optionally, for parity, the s0 module's gate §6 could add a matching named criterion (e.g. `front-door-present`) so the module text and the engine prose agree, but the engine-side prose fix alone fully closes the hole."
    },
    {
      "id": "ASF-3",
      "confirmed": false,
      "severity": "P2",
      "file": "atlas-engine/front-door/grill-idea/SKILL.md",
      "reasoning": "REJECTED. The cross-reference facts are all true, but the finding's load-bearing materiality claims are false, so the defect as described is not real.\n\nVERIFIED-TRUE (structure): grill-idea/SKILL.md line 97-98 marks `## Environments` MANDATORY for persistence/migration/external/UI surfaces; the launch-blocker gate (lines 118-126) clauses (i)+(ii) cover Surface Rulings + Required Capabilities only and omit `## Environments`; atlas.mjs line 425 PREFLIGHT FLOOR gates `## Surface Rulings` + `## Required Capabilities & Preflight` and does not enforce a non-empty `## Environments`; the `decisions` arg recipe (SKILL.md line 111) does not include `## Environments`; and S5 §6.2 (lines 201-218), web-backend.md clause 6 (lines 112-115, 200), s6 line 165, s7 line 149 all read `idea-decisions.md \"## Environments\"`.\n\nFALSE (the harm): The finding claims \"the build has the env designation NOWHERE,\" \"There is no defined fallback in s5-verify-build.md or honest-verification.md for a missing `## Environments`,\" and that the autonomous build is \"silently expected to invent\" the env. All three are wrong:\n1. A fallback IS defined at the front door: SKILL.md line 98 ends \"...whenever a persistence/migration/external/UI surface is present; OTHERWISE STATE THE REPO DEFAULT.\" Non-domain launches resolve to the repo default, not nowhere.\n2. A fallback IS defined in the consumer for an unresolved designated env. honest-verification.md lines 70-85 (blocker classification) + S5 §6.2 lines 212-218 explicitly forbid inventing/switching/mutating an env (that is a tamper → automatic `environment-scoped` FAIL) and route a missing env-config to `config-resolvable` → `verdict:BLOCKED, failedCriterion:R2_blocked_missing_config` naming the exact missing item, or `designated-env-broken` → FAIL/BLOCKED. The engine then records the blocker and degrades gracefully (DESIGN-conformant hands-off behavior). So a domain-surface launch that skipped `## Environments` does NOT yield a silent-wrong/degraded ship — it yields a corroborated, named BLOCKED with the same teeth the proposed fix would add, already present downstream. The autonomous build cannot silently invent the env; the discipline makes invention a FAIL.\n\nBecause the silent-failure scenario the finding describes cannot occur, the P2 defect is not real. The only residual value in the proposed fix is efficiency/altitude — moving an already-guaranteed BLOCKED from S5 (after spec/slice/plan/build spend) earlier to the front door. But (a) the grill already marks `## Environments` MANDATORY for domain surfaces (SKILL.md line 97), so clause (iii) largely re-states an existing obligation, and (b) the engine-side PREFLIGHT addition duplicates the downstream `environment-scoped`/`config-resolvable` gate that already has teeth. Per the honor-lean bar (DESIGN §11: teeth not ceremony, no doubled gates), adding a second gate for a condition that is already MANDATORY at the front door AND already fail-safe downstream is ceremony, not a closed silent-wrong hole. Default-reject stands: the defect is mischaracterized and immaterial."
    },
    {
      "id": "ASF-4",
      "confirmed": true,
      "severity": "P3",
      "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/front-door/atlas.md",
      "reasoning": "Mechanism verified against actual code/text. (1) The \"just go\" floor (atlas.md lines 29-30) gates the lean path entirely on refine's \"Detected Surfaces = none.\" (2) The build's S0 preflight floor (atlas.mjs lines 424-426) keys off the SAME field — it BLOCKs only when idea-decisions.md lists \"Detected Surfaces other than 'none'\" with empty Surface Rulings. So if refine under-detects a one-way surface at the pre-code idea stage, neither the front-door floor nor S0 preflight catches it. (3) The build does discover it late: DESIGN §4 allows S2/S4 to promote a missed surface to CRITICAL, and corroborate() (mjs 250-264) force-escalates any one-way surface to Tier 3, building a reversible interim + ADR. (4) That un-ruled assumption surfaces only at the end via absorb() -> report.assumptions/obstructions/adrs. So the residual the finding describes is real and the user's intent for the missed surface is genuinely never captured.\n\nThis is NOT a dead-vestige issue (no new launch-arg, no unread return field — the proposed fix is prose-only and atlas.md runs in the conversational agent, not the sandbox, so sandbox-validity is moot).\n\nMateriality: the residual itself is unavoidable and already correctly + transparently handled. The MATERIAL defect is the false reassurance in the existing prose. Lines 32-34 frame locking detected one-way surfaces as what makes \"just go\" safe, implying \"Detected Surfaces = none\" means \"no one-way doors exist.\" It actually means only \"refine spotted none\" — refine's fallible idea-stage detection is the single point of failure for the lean path, and the current text never warns the user of that. A short warning gives the user a concrete reason to decline \"just go\" on a sensitive-domain idea even when refine flagged nothing — that changes user behavior (teeth, not ceremony), so it clears DESIGN §11's leanness bar (\"if a sentence doesn't change what the agent/user does, delete it\"). Confirmed at P3/low per the finding's own grading.",
      "refinedFix": "Append one sentence to the floor paragraph at the end of atlas.md (after line 34): \"Note that `Detected Surfaces = none` means refine *spotted* none from the idea — not that none exist; refine's pre-code detection is the only thing gating 'just go.' If the idea touches a sensitive domain (money | auth | data | migrations | external | PII | destructive), do NOT take the lean path on the strength of a clean detection alone — a surface refine misses gets no Surface Ruling, and the build will silently proceed on a reversible interim that may diverge from your intent, visible only in the final report.\" This adds no gate (a pre-code refine cannot reliably satisfy one) — it gives the user a decision-changing reason to keep full grill when the domain is sensitive even if refine flagged nothing."
    }
  ],
  "synthesis": {
    "readyForDogfood": false,
    "overall": "NOT ready for dogfood: three confirmed P1 defects remain, all in the engine's S0 front-door floor and S0 re-entry logic (.claude/workflows/atlas.mjs), and every P1 blocks. The hardened S0 PREFLIGHT FLOOR — the only engine-side teeth against a direct launch that skipped grill — is currently largely ceremonial: it keys its primary trigger off a \"## Detected Surfaces\" section that does not exist in the file it names (idea-decisions.md; that field actually lives in refined-idea.md per the refine template), it is structurally blind to the absent-log case it was built for (a bypass produces no idea-decisions.md, so the condition is vacuously false and S0 writes a spec for money/auth/migration surfaces with zero rulings), and a runtime-failing front-door prerequisite drives an unconditional S0 route-back that re-runs S0 against an idea-decisions.md the engine never writes — a guaranteed no-op loop that burns the maxUpstreamRouteBacks budget. EFC-01 and LNC-01 both rewrite the same line (atlas.mjs:425) and must be applied as one coherent re-anchored floor (source surfaces from refined-idea.md \"## Detected Surfaces\" AND args, cross-check idea-decisions.md \"## Surface Rulings\" + \"## Required Capabilities & Preflight\", and treat an absent/unparseable log as itself BLOCKING when a domain surface is named); ASF-1 is a separate engine-loop guard. All fixes are sandbox-valid (prose/JS reusing already-read sources and the existing verdict:BLOCKED + routeBack envelope — no new launch-args, no new return fields). The five P2s (step-file floor contradiction, three dead-vestige envelope assertions / route-back doc mismatches, the self-declared-unreachable verifier dodge, the single-repo-default env false-FAIL) and five P3s are real polish that sharpen teeth and kill vestiges but do not block a run; fix them after the P1 floor is sound. Recommendation: land the three P1 engine fixes (one merged line-425 rewrite plus the ASF-1 helper), then dogfood.",
    "remainingFixes": [
      {
        "severity": "P1",
        "file": "/Users/cracklehat/Sites/workflow-exploration/.claude/workflows/atlas.mjs",
        "issue": "EFC-01 + LNC-01 (apply as ONE rewrite of line 425): the PREFLIGHT FLOOR mis-attributes \"## Detected Surfaces\" to idea-decisions.md (it lives in refined-idea.md), is blind to the absent-log bypass it exists to catch, and never consults args — so the front-door-skip floor silently no-fires and S0 writes a spec for unruled one-way-door surfaces.",
        "fix": "Replace the PREFLIGHT FLOOR sentence at atlas.mjs:425 with one floor that (1) builds the DOMAIN-SURFACE SET from refined-idea.md \"## Detected Surfaces\" (if present) OR the args idea/decisions text; (2) STOPs verdict:BLOCKED if that set is non-empty AND idea-decisions.md is absent/unparseable (failedCriterion:\"front-door-skipped: no idea-decisions.md for detected domain surface <x>\"); (3) STOPs BLOCKED if a surface lacks a resolved entry in idea-decisions.md \"## Surface Rulings\" (empty/placeholder/unresolved = none); (4) STOPs BLOCKED if a domain/external surface has a required capability left un-provisioned AND un-waived in idea-decisions.md \"## Required Capabilities & Preflight\". Explicitly note that \"## Detected Surfaces\" is in refined-idea.md and idea-decisions.md carries Surface Rulings + Required Capabilities. Also tighten line 424's \"(If absent, use the args context.)\" to add \"— but the PREFLIGHT FLOOR below still applies to surfaces named in the args.\" Routes through existing line 429 handling; no new launch-arg or return field. Pure prose, sandbox-valid."
      },
      {
        "severity": "P1",
        "file": "/Users/cracklehat/Sites/workflow-exploration/.claude/workflows/atlas.mjs",
        "issue": "ASF-1: a front-door-prerequisite obstruction that fails only at RUNTIME emits routeBack:S0, and both S0 escalation sites (initiative loop line 443; S7 final gate line 466) unconditionally re-run S0 against an idea-decisions.md the engine never writes — a guaranteed no-op loop that consumes maxUpstreamRouteBacks before degrading to a generic blocker.",
        "fix": "Add a helper isFrontDoorPrereqS0(rb) above runInitiative that returns true for an S0 routeBack whose check is the canonical token (front-door-prereq / capability-unavailable) and false for one-way-obstruction (engine-reclassified spec route-back), with a tightened regex fallback over rb.check+rb.reason. Apply it at BOTH S0 branches (before line 443 and before line 466): when it matches, record an actionable blocker (\"front-door prerequisite missing/failing at RUNTIME — re-run grill to provision it, then re-launch\") via note(report.blockers,...) and break/skip instead of re-running S0. Reads only routeBack.check/reason (already in the ROUTEBACK schema); pure JS, no fs/Date/Math; reuses existing note()/degrade path. Sandbox-valid."
      },
      {
        "severity": "P2",
        "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s0-spec-and-phases.md",
        "issue": "S0FLOOR-2: the engine-only PREFLIGHT FLOOR is absent from the authoritative step file, whose Â§1.2/Â§6/Â§7 narrow BLOCKED to \"genuinely unanswerable decisions\" — letting a faithful agent down-rule an unruled one-way-door surface to a recorded assumption and PASS, the exact silent substitution DESIGN non-negotiable #12 forbids.",
        "fix": "Add the floor to the step file (doc-only, reuses verdict:BLOCKED + phases:[] the engine already routes on): (1) insert a new \"## 0.5 Preflight floor (front-door bypass — hard stop, NOT a Needs-Decision)\" section before Â§1 stating the BLOCKED carve-out; (2) add a `preflight-floor` row to the Â§6 gate table marking its failure as BLOCKED-not-FAIL; (3) carve the Â§6 (\"true blocker... genuinely unanswerable\") and Â§7 (\"only genuinely unanswerable decisions\") narrowing language to add \"OR the Â§0.5 preflight-floor breach\" so Â§1.2/Â§6/Â§7 cannot reabsorb the floor into an assumption."
      },
      {
        "severity": "P2",
        "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/disciplines/honest-verification.md",
        "issue": "DVS-1: lines 235-236 assert ladderReached/verifiedKind/residualRisk \"ride on existing envelope fields — no new return fields the engine does not read,\" but the engine reads none of them (0 references; verifier schema is verdict+failedCriterion+fixNeeded+failedCriteria only) — a self-blessing dead-vestige claim that is exactly backwards and will mislead future authors.",
        "fix": "Replace honest-verification.md lines 225-236 so the three fields are documented as written to the on-disk verification artifact (build-verification.md / verification.md) that later steps re-read, and state explicitly the engine does NOT read them off the return — the ladder result reaches the gate by collapsing into verdict (PASS only at the mandatory rung) and failedCriterion (names the unreached rung). Delete only the false \"ride on existing envelope fields\" sentence; PRESERVE the accurate no_substitution/obstacle-ledger reconciliation clause. Pure prose."
      },
      {
        "severity": "P2",
        "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s7-verify-main-spec.md",
        "issue": "EFC-02: line 151 + lines 158-161 promise that disciplines_attested absence routes back to S4 and the engine \"re-enters the target and cascades,\" but at S7 the engine re-enters ONLY an S0 route-back (line 466); an S4/S1/S2/S3 final-gate route-back falls through to a degraded blocker (line 471) — a dead-vestige route the engine never honors at this step.",
        "fix": "Edit the step file so the documented route matches the degrade-only engine (no engine change): rewrite line 151 to state disciplines_attested absence is verdict:FAIL with failedCriterion that does NOT rebuild at S7 (only an S0 route-back is re-entered; an S4 route-back is recorded as a blocker and the run degrades, ready stays false — fix the slice on disk and re-launch); rewrite the FAIL bullet (156-161) to keep S0 as the one honest S7 route-back and drop the false S1-S4 re-entry/cascade promise."
      },
      {
        "severity": "P2",
        "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s5-verify-build.md",
        "issue": "EFC-03: the BLOCKED/code-verified escape from the behavior-test FAIL loop is grounded only by verifier self-attestation (a generic \"genuinely unrunnable\" checklist) and never cross-references idea-decisions.md \"## Required Capabilities & Preflight\" — so a slice can self-declare a provisioned-with-probe capability \"unreachable\" to dodge writing the real test, contradicting the front-door ledger unchallenged.",
        "fix": "In s5-verify-build.md Â§6.3, before a slice may take the BLOCKED/code-verified escape, require cross-checking the claimed-unreachable capability against idea-decisions.md \"## Required Capabilities & Preflight\": if it is provisioned?=yes OR carries a how-to-verify-present probe, the verifier MUST run the probe; a passing probe => claim is false => behavior-test-present FAIL (verdict:FAIL, routeBack:null — owed real test), not BLOCKED. Only an un-provisioned / waived-with-fallback / probe-less capability may ground the BLOCKED escape. Reads an already-granted on-disk file; no new field. Sandbox-valid."
      },
      {
        "severity": "P2",
        "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s5-verify-build.md",
        "issue": "LNC-02: the unconditional `environment-scoped` S5 gate (Â§6.2) presupposes idea-decisions.md \"## Environments\" names an env, but grill legitimately omits a DEV/TEST split for a non-UI/non-domain STANDARD slice (repo default only) — so a literal verifier has a documented wrong-env hook to false-FAIL/BLOCK an honest slice that ran against the repo default.",
        "fix": "Two-site prose mirror: in s5-verify-build.md Â§6.2 (after the \"scoped to the environment named in ## Environments\" sentence) and in honest-verification.md no_substitution R2 block (after the DEV/TEST bullets), add a \"no declared split => the single repo default IS the proper env\" escape: record that one default per activity and pass; a missing ## Environments split is not itself a wrong-env tamper; environment-scoped FAILs only on an observed switch/mutation AWAY from the declared-or-default env. Preserves the existing action-based FAIL teeth verbatim."
      },
      {
        "severity": "P3",
        "file": "/Users/cracklehat/Sites/workflow-exploration/.claude/workflows/atlas.mjs",
        "issue": "S0FLOOR-1: SCHEMA_S0 (line 127-128) makes `phases` a required top-level field, but the PREFLIGHT FLOOR text (line 425) tells a BLOCKED S0 to \"do NOT write the spec\" without saying it must still emit phases:[] — an agent that omits the key fails structured-output validation and forces one self-healing re-prompt on the clean-stop path. (Capped at one re-prompt; line 430 already does s0.phases||[]; do NOT mirror to the step file — the floor does not exist there.)",
        "fix": "In atlas.mjs:425, change \"and do NOT write the spec.\" to \"and do NOT write the spec — but STILL return phases:[] (SCHEMA_S0 requires the field; emit it empty so the BLOCKED stop validates without a re-prompt).\" Single-location prose edit; do not touch s0-spec-and-phases.md (no preflight floor there; Â§6/Â§7 already treat phases as a standard return field)."
      },
      {
        "severity": "P3",
        "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s7-verify-main-spec.md",
        "issue": "DVS-3: the return-envelope block header (line 228) claims \"the engine reads this\" while listing shipSafe:boolean (240) and a receipt:{...} object (241) that SCHEMA_VERDICT never parses — two dead vestiges; ship-safety reaches the engine only via verdict:PASS and the receipt is already destined for on-disk prose.",
        "fix": "Delete the shipSafe and receipt lines (240-241) from the return-envelope code block; relabel the header (228) to \"## Return envelope (the verifier emits this; the engine routes on the standard-envelope fields — verdict/failedCriterion/routeBack/artifactPath/assumptions/blockers/surfaces/obstruction; DESIGN Â§3)\". Leave line 234's failedCriterion enum value \"shipSafe\" untouched (informational named gate criterion). Pure prose."
      },
      {
        "severity": "P3",
        "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s6-verify-phase.md and /Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s7-verify-main-spec.md",
        "issue": "XFC-1: both attestation clause (b)s require a behavior test \"that passes\" with no exception and make its absence a disciplines_attested FAIL routeBack:S4, contradicting s4 criterion 7 / s5 Â§6.1 / honest-verification.md, which let an external-genuinely-unavailable slice ship a committed skip-with-reason test under BLOCKED/code-verified — so an honest external-unavailable slice that passed S4/S5 is spuriously FAILed (false-negative; not the infinite loop the original finding claimed).",
        "fix": "In both clause (b)s (s6:161-163, s7:145-147), fold in the s4/s5 exception: a real-entry-point behavior test that passes OR, for a slice S5 recorded external-genuinely-unavailable, the SAME test committed skip-with-reason naming the exact blocker, that slice BLOCKED/code-verified with residual risk listed (a skip-with-reason on any slice NOT so classified is still a disciplines_attested FAIL). The trailing \"(a)/(b)/(c) absent -> FAIL routeBack:S4\" lines need no change. Prose-only; reads on-disk build-verification.md verdict + the committed test."
      },
      {
        "severity": "P3",
        "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s5-verify-build.md",
        "issue": "EFC-04: Â§6.2 frames `environment-scoped` around a DEV/TEST split and flags env-switching only via a soft \"for convenience\" motive judgment; for a STANDARD slice whose ## Environments is a bare repo default there is no named anchor making a mid-slice env switch falsifiable — exactly where switch-to-green hides.",
        "fix": "In s5-verify-build.md Â§6.2, after the existing \"switching envs for convenience is a tamper\" sentence, add: when ## Environments names only a repo default (no DEV/TEST split), environment-scoped binds to a consistency invariant — resolve the single default once and every activity's per-activity stamp (base URL/host/build SHA/data tier/simulator target, already required) must show the SAME resolved env; any change between activities absent a recorded reason is a wrong-env tamper => automatic environment-scoped FAIL (routeBack:null, fixNeeded names the switching activity). Pure prose; checkable from artifacts the verifier already produces."
      },
      {
        "severity": "P3",
        "file": "/Users/cracklehat/Sites/workflow-exploration/atlas-engine/steps/s5-verify-build.md",
        "issue": "EFC-05: on a FAST-lane STANDARD slice S3 never runs, so S2's self-graded follow-the-house-pattern branch (b) \"divergence-justified\" has no plan-time second party; S5 already owns the reuse call via no_substitution but Â§6.3 never explicitly names the branch-(b) SIBLING-hit-with-divergence case, leaving an implicit catch instead of a diff-checkable reconcile item.",
        "fix": "Sharpen (do not add a criterion) the EXISTING no_substitution reconcile in s5-verify-build.md Â§6.3: after the substituted/hand-applied-prerequisite paragraph, add a \"Divergence-reconcile\" clause — when the grounding note records a SIBLING: hit AND the plan took branch (b), re-grade the justification yourself; it passes only if the divergence names a concrete contract/lifecycle/shape difference the sibling cannot carry, else the fresh code is a hand-rolled reimplementation => no_substitution FAIL (routeBack:null; fixNeeded: re-plan onto the sibling). Reads build.md grounding note + plan.md (already read); rides the existing failedCriterion. Sandbox-valid."
      }
    ]
  }
}