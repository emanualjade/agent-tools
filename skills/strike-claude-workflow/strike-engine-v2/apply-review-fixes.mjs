export const meta = {
  name: 'apply-strike-v2-review-fixes',
  description: 'Apply the P2/P3 module fixes from the hardening adversarial review (5 files: s0 step file floor, honest-verification dead-vestige + env-default, s5 probe-cross-check + env-default + consistency + divergence, s7 degrade-route + dead-vestige + external-unavailable exception, s6 external-unavailable mirror).',
  phases: [{ title: 'Apply', detail: '5 per-file edits with shared mirror wording' }],
}

const ROOT = '/Users/cracklehat/Sites/workflow-exploration/strike-engine-v2'
const ST = (n) => ROOT + '/steps/' + n
const D = (n) => ROOT + '/disciplines/' + n

const SHARED = 'TWO CROSS-FILE MIRRORS — use this EXACT wording wherever your file needs it, so both sides match:\n' +
  '• ENV-DEFAULT ESCAPE (honest-verification no_substitution R2 block AND s5 §6.2): "No declared DEV/TEST split: when idea-decisions.md \'## Environments\' names only a repo default (legitimate for a non-UI / non-domain STANDARD slice), that single default IS the proper environment — record it once per activity and pass; a missing DEV/TEST split is NOT itself a wrong-env tamper. `environment-scoped` FAILs only on an OBSERVED switch/mutation AWAY from the declared-or-default env."\n' +
  '• EXTERNAL-UNAVAILABLE EXCEPTION (s7 AND s6 `disciplines_attested` clause-(b)): "Exception: a slice S5 recorded `external-genuinely-unavailable` satisfies this with the SAME real-entry-point test committed `skip-with-reason` naming the exact blocker (that slice BLOCKED/code-verified with residual risk listed) — not a FAIL. A skip-with-reason on a slice NOT so classified is still a disciplines_attested FAIL."\n\n' +
  'RUNTIME FACTS: the engine reads only the step result `verdict`/`failedCriterion`/`routeBack` (+ the per-step structured fields) and routes on verdict (PASS/FAIL/BLOCKED) + routeBack; it does NOT read ladderReached/verifiedKind/residualRisk/shipSafe/receipt — those reach later steps via the ON-DISK verification artifact (build-verification.md / verification.md). No new launch-args or return fields. Lean: teeth, not ceremony. Preserve each file\'s structure/voice; make surgical edits only.'

const RESULT = { type: 'object', required: ['file', 'changed'], properties: { file: { type: 'string' }, changed: { type: 'boolean' }, summary: { type: 'string' } } }

const EDITS = [
  { file: ST('s0-spec-and-phases.md'), instr: 'S0FLOOR-2 (P2): the engine has a PREFLIGHT FLOOR (returns verdict:BLOCKED on a front-door bypass: a domain surface from refined-idea.md "## Detected Surfaces"/args is present but idea-decisions.md is absent/unparseable, OR "## Surface Rulings" is empty/placeholder, OR a domain/external capability is un-provisioned & un-waived in "## Required Capabilities & Preflight"). This floor is ABSENT from this authoritative step file, whose §1.2/§6/§7 narrow BLOCKED to "genuinely unanswerable decisions" — letting a faithful agent down-rule an unruled one-way-door surface to a recorded assumption and PASS (the exact silent substitution DESIGN non-negotiable #12 forbids). FIX (doc-only, reuses verdict:BLOCKED + phases:[] the engine already routes on): (1) insert a new "## 0.5 Preflight floor (front-door bypass — a HARD STOP, NOT a Needs-Decision)" section before §1 stating the BLOCKED carve-out exactly as the engine floor does (the three trigger conditions; STILL emit phases:[] on BLOCKED); (2) add a `preflight-floor` row to the §6 gate table marking its breach as BLOCKED (not FAIL, not an assumption); (3) carve the §6 ("a true blocker... genuinely unanswerable") and §7 ("only genuinely unanswerable decisions") narrowing language to add "OR a §0.5 preflight-floor breach" so §1.2/§6/§7 cannot re-absorb the floor into an assumption.' },
  { file: D('honest-verification.md'), instr: 'TWO fixes. (1) DVS-1 (P2): the current text (around the "What this step emits" / envelope block, ~lines 225-236) claims ladderReached/verifiedKind/residualRisk "ride on existing envelope fields — no new return fields the engine does not read." That is BACKWARDS — the engine reads NONE of those three. FIX: rewrite so they are documented as WRITTEN TO THE ON-DISK verification artifact (build-verification.md / verification.md) that later steps re-read, and state the ladder result reaches the gate by collapsing into `verdict` (PASS only at the mandatory rung) + `failedCriterion` (names the unreached rung). DELETE only the false "ride on existing envelope fields / no new return fields" sentence; PRESERVE the accurate no_substitution + obstacle-ledger reconciliation clause. (2) LNC-02 (P2): in the `no_substitution` R2 block (after the DEV/TEST env bullets), add the ENV-DEFAULT ESCAPE (shared wording above) so a STANDARD slice that ran against a bare repo default is not false-FAILed.' },
  { file: ST('s5-verify-build.md'), instr: 'FOUR fixes. (1) EFC-03 (P2): in §6.3, BEFORE a slice may take the BLOCKED/code-verified escape from behavior-test-present, require cross-checking the claimed-unreachable capability against idea-decisions.md "## Required Capabilities & Preflight": if it is `provisioned?=yes` OR carries a `how-to-verify-present` probe, the verifier MUST run the probe; a passing probe => the "unreachable" claim is FALSE => behavior-test-present FAIL (verdict:FAIL, routeBack:null — the real test is owed), NOT BLOCKED. Only an un-provisioned / waived-with-fallback / probe-less capability may ground the BLOCKED escape. (2) LNC-02 (P2): in §6.2, after the "scoped to the environment named in ## Environments" sentence, add the ENV-DEFAULT ESCAPE (shared wording above). (3) EFC-04 (P3): in §6.2, after the existing "switching envs for convenience is a tamper" sentence, add: when ## Environments names only a repo default (no DEV/TEST split), `environment-scoped` binds to a CONSISTENCY INVARIANT — resolve the single default once; every activity\'s per-activity env stamp (base URL/host/build SHA/data tier/simulator target, already required) must show the SAME resolved env; any change between activities absent a recorded reason is a wrong-env tamper => automatic environment-scoped FAIL (routeBack:null, fixNeeded names the switching activity). (4) EFC-05 (P3): SHARPEN the existing no_substitution reconcile in §6.3 (do NOT add a criterion): after the substituted/hand-applied-prerequisite paragraph, add a "Divergence-reconcile" clause — when the grounding note records a `SIBLING:` hit AND the plan took branch (b) (divergence-justified), the S5 verifier re-grades the justification itself; it passes only if the divergence names a concrete contract/lifecycle/shape difference the sibling cannot carry, else the fresh code is a hand-rolled reimplementation => no_substitution FAIL (routeBack:null; fixNeeded: re-plan onto the sibling). This closes the FAST-lane gap where S3 never second-parties S2\'s self-graded divergence.' },
  { file: ST('s7-verify-main-spec.md'), instr: 'THREE fixes. (1) EFC-02 (P2): the disciplines_attested FAIL currently promises routeBack:S4 and that the engine "re-enters the target and cascades", but at S7 the engine re-enters ONLY an S0 route-back — an S4/S1/S2/S3 final-gate route-back DEGRADES to a recorded blocker (ready stays false), it is NOT re-entered. FIX: rewrite the disciplines_attested FAIL routing + any "engine re-enters/cascades" promise so it matches the degrade-only engine: S0 is the ONE honest S7 route-back; a disciplines_attested gap is recorded as a blocker and the run DEGRADES (ready stays false — fix the slice on disk and re-launch), NOT an S7 re-build. Drop the false S1-S4 re-entry/cascade promise at this step. (2) DVS-3 (P3): in the return-envelope code block, DELETE the `shipSafe: boolean` and `receipt: {...}` lines (the engine\'s SCHEMA_VERDICT never parses them — dead vestiges; ship-safety reaches the engine via verdict:PASS, the receipt is on-disk prose), and relabel the block header to "## Return envelope (the verifier emits this; the engine routes on the standard-envelope fields — verdict/failedCriterion/routeBack/artifactPath/assumptions/blockers/surfaces/obstruction; DESIGN §3)". Leave the failedCriterion enum value "shipSafe" (informational). (3) XFC-1 (P3): in disciplines_attested clause (b) (the "behavior test that passes" requirement), fold in the EXTERNAL-UNAVAILABLE EXCEPTION (shared wording above).' },
  { file: ST('s6-verify-phase.md'), instr: 'XFC-1 (P3): in the mirrored single-phase `disciplines_attested` clause (b) (the "behavior test that passes" requirement), fold in the EXTERNAL-UNAVAILABLE EXCEPTION (shared wording above), identically to s7, so an honest external-genuinely-unavailable slice that passed S4/S5 under skip-with-reason + BLOCKED/code-verified is not spuriously FAILed at the single-phase final gate.' },
]

phase('Apply')
log('Applying P2/P3 review fixes across ' + EDITS.length + ' files...')
const results = (await parallel(EDITS.map((e) => () =>
  agent('Apply these surgical fixes to ONE Strike v2 module. Read the file in full first, then edit in place (preserve structure/headers/voice).\n\n' + SHARED + '\n\nFILE: ' + e.file + '\n\nFIXES:\n' + e.instr + '\n\nReturn a short summary of what you changed.',
    { label: 'fix:' + e.file.split('/').pop(), phase: 'Apply', schema: RESULT })
))).filter(Boolean)
return { results, applied: results.filter((r) => r.changed).length }
